# Memory · PymeWebPro Studio

This file is the hot cache. Claude reads this first on every session in `~/code/pymewebpro/`.
Full glossary and rich context live in `./memory/`.

## HARD RULE: Colombia-only market, COP-only pricing

**PymeWebPro serves Colombian SMBs only.** This is a Colombian company, billed in COP, marketed to Colombian customers, operating from Medellín.

**The demographic is Colombian. ALWAYS quote money in Colombian pesos (COP), and ONLY in COP.** This rule applies to:
- Every page on pymewebpro.com (Spanish and English versions)
- Every blog post, guide, or article we publish
- Every chat response when discussing pricing, costs, budgets, examples, or competitor benchmarks
- Every client proposal, invoice, email, social post

A Colombian reader does NOT need a USD conversion. Don't include one. When citing international tool costs (Ahrefs, SEMrush, Meta Ads, Beehiiv, etc.), do the conversion to COP at the prevailing TRM (roughly $4.000 COP/USD as of mid-2026) and quote ONLY the COP figure. No "(~$129 USD)" parenthetical, no "USD" label, no mixed-currency tables. Pure COP, period.

The only exception: industry-standard ratio statistics where the unit cancels out (e.g. "email ROI is roughly $36 to $42 per $1 spent") can remain as a ratio without forcing a COP conversion, because the figure is a ratio not a price the reader pays.

Do NOT:
- Quote prices in CAD or USD as the primary unit on any pymewebpro asset
- Position the studio as serving NA / Canada / US / LATAM clients
- Mention London, Ontario as a business location
- Frame pymewebpro as bilingual for "two markets"
- Reference Wise, Stripe, or NA payment rails as our payment methods

DO:
- Quote prices in COP, with full thousand-separator formatting (`$390.000 COP`, `$20 million COP`)
- Treat Mike's Canadian nationality as a personal fact about the founder, not a market positioning
- Treat any English copy on pymewebpro as a courtesy for English-speaking Colombian residents / expats, not as an NA-market funnel
- Reference Wompi (cards, PSE, bank transfer) as the payment method
- Frame the studio as "Colombian web design studio" or "diseño web en Colombia"

## Me

**Michael Chartrand** ("Mike") · Canadian citizen by nationality, full-time resident of Medellín, Founder & Legal Representative of **Norte Sur Consulting S.A.S.**, doing business as **PymeWebPro**.
- **Legal entity:** Norte Sur Consulting S.A.S. (Colombian Sociedad por Acciones Simplificada)
- **NIT:** 901.956.771-1
- **DBA / brand:** PymeWebPro
- **Based in:** Medellín, Antioquia, Colombia
- **Email:** mike@mikec.pro

PymeWebPro is the public-facing brand and trade name. Norte Sur Consulting S.A.S. is the contracting legal entity on all invoices, contracts, banking, and tax filings.

**Two-person partnership.** Mike (Canadian founder by nationality, legal-entity owner, Medellín resident) and **Santiago Santos** (paisa, born and based in Medellín, profit-share partner) run the studio together. Both are in Medellín and have access to client records. Default routing:
- **Spanish-first / Colombian buyers** → Santiago (WhatsApp +57 301 404 7722, primary).
- **English-language Colombian / expat buyers in Colombia** → Mike (Cal.com 15-min call, WhatsApp).
- Either can hand off to the other if the situation calls for it.

Colombian web design studio targeting Colombian SMBs.

Do NOT describe the studio as "solo operator" or imply Mike works alone · Santiago is a real, named partner who shares profit and is publicly named in the FAQ + privacy policy as a person with access to client records.

## Studio · One-line

PymeWebPro = a Medellín-based, AI-leveraged web design studio that ships custom-coded sales pages on Cloudflare for Colombian SMBs at a fraction of agency pricing.

## Location

- **Medellín, Antioquia, Colombia** · studio · design, code & deploy · Colombian clients billed in COP via Wompi.

Single location. No London office. Mike's family home is in London, Ontario but that is a personal fact, not a business location and must not appear on pymewebpro public surfaces.

## Pricing (current · single-product model as of 2026-05-20)

ONE product plus an à la carte add-on menu. No Essential/Pro tiers.

| Product | Price | Includes |
|------|-------|----------|
| La página de ventas | $390.000 COP one-time (IVA incl.) | custom 6-step conversion page, 1 primary CTA, WhatsApp/form + click-to-call, Google Maps, booking integration, testimonials, SEO, privacy-first analytics, domain+SSL setup, 1 month hosting+support, 2 revision rounds, live ~48h |

Add-ons (à la carte, IVA incl., source of truth `~/code/pymewebpro-sales/internal/add-on-menu.md`): Página adicional $50.000 (máx 3) · Versión bilingüe ES/EN $75.000 · Copywriting $150.000 · Catálogo simple máx 30 $180.000 · Perfil de Empresa de Google $90.000 · Analítica avanzada (GA4+GTM+Pixel) $120.000 · Integración CRM $190.000 · Chatbot de captura $190.000 once + $80.000/mes · Botón de descarga de PDF $50.000 · Ronda de revisión adicional $90.000. A real store and a blog are out of scope but quotable as custom.

## Hosting

- Included: 1 month with the base page.
- After that, prepaid: **$30.000 COP/mes** or **$300.000 COP/año** (saves two months vs $360.000). Optional, not a forced subscription.
- "Support" = keeping the page online and patched, not unlimited edits. Edits after the free revision window bill as additional revision rounds ($90.000 c/u).
- Chatbot add-on carries its own run fee: $80.000 COP/mes (or $800.000/año prepaid), ~300 conversations/mes then overage. The only thing that adds a monthly to the page.

## Payment terms

- **30% deposit to start** ($117.000 on the base page) · no design or development begins until deposit clears
- **70% on launch** · site sits on staging URL; DNS to client domain not connected until balance clears
- Payments via **Wompi** (cards, PSE, bank transfer) in COP only
- **30-day money-back guarantee post-launch:** site goes offline + full refund within 14 days if client unhappy. After 30 days, fee is non-refundable.

Switched from 50/50 to 30/70 on 2026-05-05. Lower psychological barrier to start (productized service, not custom agency build), DNS gating on the 70% balance still protects against non-payment.

→ Full strategy + economics: `memory/context/studio.md`

## Brand identity

| Mark | Use |
|------|-----|
| `<pymewebpro/>` | Site header, footer, email, invoices |
| `<pwp/>` | Favicon, social profile pic, app icon |
| `#FF5C2E` | Single accent (orange) · bracket color in small format |
| Inter Tight + JetBrains Mono | Display / mono pair |
→ Full design system: `memory/context/brand.md`

## Pipeline · "manual mockup"

Build process for every client site:
1. Write self-contained HTML in `manual-mockups/<slug>/index.html`
2. Run `node portal/scripts/rebuild-mockups.mjs` · auto-discovers wired mockups from `portal/src/manual-mockups.js` imports and regenerates each `portal/src/manual-mockups-<slug>.js` module from its source HTML (base64-embeds local jpg/png references, applies template-literal escapes). Avoids the trap of hardcoded site lists going stale.
3. New site only: add the import + map entry to `portal/src/manual-mockups.js`, add the file to `portal/package.json` `check:worker` script
4. `npm run check` → verify
5. `npm run deploy` → goes live at `mockups.pymewebpro.com/<slug>/`
→ Full pipeline: `memory/context/pipeline.md`

## Prospect mockups · build playbook for Claude in Cowork

When Mike says `build mockup for <slug>`, this is the procedure. The studio admin captures the brief in D1; Cowork (you, Claude) does the actual design + build work. Each mockup is bespoke, never template-driven · Mike will reject anything that looks recycled from another build.

### Step 1 · Get the brief

The brief lives in D1 table `mockup_prospects` (uuid 93fc7e21-713c-4479-bb55-69ae05c275dc on the `pymewebpro-portal` db) and includes: business_name, instagram_url, facebook_url, tiktok_url, website_url, style_brief, owner_name, owner_whatsapp, cal_link, notes, chatbot_system_prompt, status. Claude in the Cowork sandbox CANNOT query D1 directly (no Cloudflare creds, admin API is Access-gated). Two paths to obtain the brief:
- Ask Mike to copy-paste it. Fastest.
- Have him export from the studio admin at `colguides.com/portal/pymewebpro/admin/mockups/<slug>`.

If `notes` has content, READ IT FIRST. That's Mike's running feedback log from prior iterations.

### Step 2 · Study the brand

Always do this before designing. Skipping it is what produces template-y output.
- WebFetch usually fails on Instagram/Facebook/TikTok (JS-rendered shells). Either ask Mike for screenshots of the IG profile + 3-4 grid posts + a story, or use the Claude-in-Chrome MCP if the extension is installed.
- Their existing website (if any) WebFetches fine and tells you their actual copy voice, layout instincts, what they highlight.
- Extract from sources: logo + palette (use the actual hex of the dominant color, not "green"), typography vibe in their graphics, photography style (photo-driven vs type-and-icon driven), voice (formal vs warm, paisa-friendly vs neutral), what services they emphasize, brand archetype (caregiver, expert, friend, etc).
- Confirm with Mike before designing: address, hours, services, anything the owner specifically wants featured. Don't fabricate operational details · clearly placeholder anything you don't know.

### Step 3 · Pick the design direction

- Spanish primary. Only add English secondary if the brief explicitly says so (expat-facing).
- Pick a type pairing distinct from existing mockups so the studio's work doesn't look interchangeable. Current pairings in use: see `manual-mockups/*/index.html`. Avoid copying.
- Pick 4-6 brand colors with explicit hex values. Don't reach for the generic teal/coral palette by default.
- Decide photo vs type-and-icon driven. If the brand's IG is graphic-heavy (badges, callouts, illustration), type-and-icon often beats photo. If they shoot a lot of product/people, photo-driven works.
- The "voice" of CTAs and copy must match the brand archetype. A caregiver pharmacy ("Tu droguería de confianza") doesn't speak the same way as a parfumerie ("Vestida para arruinarles la noche").

### Step 4 · Build the bespoke HTML

Write `manual-mockups/<slug>/index.html`. Hard constraints:
- Fully self-contained: inline CSS, optional inline JS, no external CSS framework.
- Allowed externals: Google Fonts preconnect + stylesheet, the brand's logo file embedded via `rebuild-mockups.mjs` base64 pipeline.
- No em dashes anywhere in user-visible text.
- Mobile-first, breakpoint at ~780-880px depending on layout.
- Honor `prefers-reduced-motion`, include `@media print` cleanup, semantic HTML.
- JSON-LD: at minimum LocalBusiness + relevant Service.
- Chat widget mandatory: floating fab + slide-up panel + JS that POSTs to `/api/chat/<slug>` and handles `[ORDER]` / `[HANDOFF]` markers → wa.me link. Reuse the structure from `manual-mockups/central-farma-drogueria/index.html`.
- All visible CTAs route to the chat agent (`data-action="open-chat"`), NOT directly to WhatsApp. Contact info cards and footer can show WhatsApp as data.

### Step 5 · Image handling

Gemini Imagen generates good photos but mangles embedded text (logos on signage, license plates, etc). When Mike provides images:
- They land as PNGs from Imagen. Compress to JPG in the sandbox:
  ```bash
  cd /sessions/bold-keen-davinci/mnt/code/pymewebpro/manual-mockups/<slug>
  for f in hero-*.png; do
    convert "$f" -resize "1000x>" -strip -quality 82 -interlace JPEG "${f%.png}.jpg"
  done
  ```
  Targets: ~100-200KB per JPG, max width 1000px, quality 82.
- Logo files: keep as PNG (transparency matters), ~100KB or smaller. Use it via `background-image:url('./logo-mark.png')` in CSS so the rebuild script base64-embeds it ONCE and every touchpoint reuses it via the class.
- For per-photo brand overlays (CF badge on a delivery crate, fake clean license plate), use absolutely-positioned overlay elements scoped to a specific slide via `[data-slide="N"]` attribute selectors. Be ready to iterate on percentage positioning from screenshots Mike sends. Don't guess pixel-precise on the first try.

### Step 6 · Lessons learned (gotchas to avoid)

- **`aspect-ratio` collapses to zero when all children are `position:absolute`.** Use the `padding-top:<ratio>%` trick instead (e.g. `padding-top:125%` for 4:5). The CF gallery silently rendered at zero height for half a day because of this.
- **Imagen embeds garbled text in plates/signs.** Don't ask Imagen to render specific brand text (it auto-completes to plausible existing brands · "CF" became "COLOMBIA FAST CF"). Generate clean blank surfaces and overlay branding via CSS.
- **CSP set by `withSecurityHeaders` in `portal/src/index.js`** restricts what scripts can load. Cloudflare's Web Analytics beacon is allowed via the explicit `static.cloudflareinsights.com` addition. If you load any new third-party (analytics, fonts beyond Google, etc), update the CSP allowlist there.
- **Heavy base64 bundles are fine.** Existing mockups range from 28KB to 1.7MB. Lighthouse doesn't penalize inline images the way it penalizes blocking HTTP waterfalls.
- **Stale "por WhatsApp" copy.** When the agent is the front door, every "escríbenos por WhatsApp" and "pídenos por WhatsApp" in user-visible copy must be swapped. Keep WhatsApp framing only in (a) the chat widget's error fallback and footer disclaimer, (b) contact-info cards as data.
- **The studio admin's auto-synthesized chatbot prompt is too thin.** Once a mockup is past the first draft, populate `chatbot_system_prompt` with real (or plausibly-made-up-for-mockup) FAQ data: products carried, prices, coverage zones, services. Otherwise the agent escalates every specific question to WhatsApp, defeating its own purpose.

### Step 7 · Quality gate (blocks deploy)

```bash
cd ~/code/pymewebpro/portal
node scripts/check-standards.mjs
```

Must return 0 FAILs. WARN-level issues (Organization JSON-LD regex, og:title, NIT block for Colombian sites) are acceptable as long as they're considered. Production mockups generally have 2-3 standing WARNs.

### Step 8 · Wire into the registry

For a NEW slug:
1. Append `import { <camelCase>Html } from "./manual-mockups-<flatslug>.js";` to `portal/src/manual-mockups.js`.
2. Add the entry to the `MANUAL_MOCKUPS` map in the same file.
3. Create an empty placeholder file at `portal/src/manual-mockups-<flatslug>.js` so the rebuild script has somewhere to write.
4. Add the slug to `toolListMockups()` in `portal/src/chief-of-staff.js` so the studio's chief-of-staff agent knows about it.

For an EXISTING slug being re-built (iteration): no registry changes needed.

### Step 9 · Rebuild + deploy

```bash
cd ~/code/pymewebpro/portal
node scripts/rebuild-mockups.mjs          # base64-embed images, regenerate bundle
node scripts/check-standards.mjs           # block on FAILs
wrangler deploy                            # ~5s to ship
cd .. && git add -A && \
  git commit -m "<slug>: <short summary of what changed>" && \
  git push                                 # for posterity
```

Live URL: `https://mockups.pymewebpro.com/<slug>/`.

### Step 10 · Confirm with Mike + log

- Paste the live URL + a 3-line summary of what shipped (sections, visual direction, anything to verify on his end).
- Tell Mike to flip the prospect's `status` from `brief` to `live` in the studio admin once the URL renders correctly. This keeps the listing honest AND lets the "Copy build prompt" button auto-detect `revise` vs `build` on next iteration.
- Add a Recent changes entry at the top of `~/code/pymewebpro/CLAUDE.md`. Date, slug, files touched, deploy command, any follow-ups.
- The first build is rarely the last. Mike iterates via the `notes` textarea on the prospect's admin row. Next session reads those notes first.

### Step 11 · Generic chatbot

The chat widget on every prospect mockup talks to `mockups.pymewebpro.com/api/chat/<slug>` (handler at `portal/src/prospect-chat.js`). It auto-synthesizes a Spanish system prompt from the brief unless `chatbot_system_prompt` is set on the D1 row. After the first build:
- Draft a real FAQ-style system prompt as a `chatbot-prompt.md` file alongside the index.html (so it's checked into git).
- Tell Mike: `cat manual-mockups/<slug>/chatbot-prompt.md | pbcopy`, then paste into the System prompt textarea in studio admin, save.
- The agent picks it up on next request (no deploy needed; D1 reads are live).

## Production marketing site

The studio's primary site at **pymewebpro.com** is Spanish-primary, Colombian-market only. Served by Cloudflare Pages directly from the repo root: **`index.html`** (Spanish, primary) and **`en/index.html`** (English secondary). Edit those files directly. There is no longer a `manual-mockups/pymewebpro-v4/` build-source folder, no `scripts/build-bilingual.mjs`, no `npm run build:bilingual` step. `check:standards` (run by `npm run check`) validates both root files in-place.

ES (primary): single product, la página de ventas $390.000 COP + à la carte add-ons, Wompi payments, Colombian-market copy.
EN (secondary courtesy): optional English version for English-speaking Colombian residents and expats. Same COP pricing. Same Colombian-market positioning. NOT an NA-market funnel.

Single footer with Medellín location and the Norte Sur NIT block.

**Section 4 "La Prueba" testimonials** ship three Google reviews in a 3-column grid at desktop/tablet (single column on mobile): Patrick Detzner (Schedulator), Orlando de La Flor Ferrari (Blue Whale International), Santiago Florez (Inviersol).

**`builds-grid` below the reviews** points at the public-facing URL for each build. Live customer sites use their real domain (Schedulator → schedulator2.vercel.app, Inviersol → inviersol.com), concept mockups use `mockups.pymewebpro.com/<slug>/`.

## Active client mockups (6 wired)

Three former entries (`schedulator`, `inviersol`, all `pymewebpro-*` variants) were removed from the mockups registry on 2026-05-18 because they're either now live on their own domains or no longer needed.

| Slug | Brand | Sector | Status |
|------|-------|--------|--------|
| `blues-kitchen` | The Blues Kitchen | Events venue | Live, photos embedded |
| `daga-parfum` | Daga Parfum | Luxury fragrance e-comm | Live |
| `blue-whale-international` | BWI Talent | Finance + tech recruiting | Live, refocused 2026-05 |
| `espacio-dental` | Espacio Dental | Dental clinic (expat-targeted) | Live |
| `marena` | Marena | Fashion retail (cold outreach) | Live |
| `start` | PymeWebPro /start | Tutorial walkthrough page | Live |
→ Per-client briefs: `memory/projects/`

Plus three `medellin-guide*` variants for the sibling guide network.

All sites with a navbar (i.e. all except `start`) ship a **working hamburger drawer**: 3-bar toggle → animated X, slide-in `.nav-drawer` from right with brand-tinted scrim, body scroll lock, close on link/scrim/Escape. Brand colors per site via `--accent`, `--ink`, `--line` vars.

## Tech stack

- Cloudflare Workers (single worker `pymewebpro-portal`)
- D1 (SQLite), R2 (storage), KV
- Wrangler CLI · `npm run deploy`
- Worker source: `~/code/pymewebpro/portal/src/`
- Manual mockup HTML sources: `~/code/pymewebpro/manual-mockups/`

### Master portal subpath mount (2026-05-13)

The same `pymewebpro-portal` worker also serves the studio admin under the master portal at **colguides.com/portal/pymewebpro/*** (Mike + Santi only, Cloudflare Access-gated). Mechanism:

- Wrangler route `colguides.com/portal/pymewebpro/*` directs traffic to this worker.
- The fetch handler detects `colguides.com` host + the `/portal/pymewebpro` prefix, strips it from `url.pathname`, reconstructs the Request, and lets the existing router see root-relative paths unchanged.
- After the response is built, `rewriteForPwpSubpath()` rewrites Location redirects and HTML `href`/`action`/`src` attributes to prepend the prefix, and injects `<script>window.PWP_BASE="/portal/pymewebpro";</script>` into HTML responses.
- The SPA reads `window.PWP_BASE` via the `pwpBase()` / `pwpStripBase()` / `pwpPush()` / `pwpReplace()` helpers at the top of the inlined React app. All fetch helpers (`api`, `adminApi`), direct `fetch('/api/...')` calls, and the client-side router prepend the base on writes and strip it on reads.

Adding a new direct `fetch('/api/...')` or `window.location.href = '/...'` anywhere in the SPA? Always wrap the path: `pwpBase() + '/api/...'`. Otherwise it'll 404 under the master portal mount while still working on `portal.pymewebpro.com`.

`portal.pymewebpro.com` is retained as the legacy URL during the cutover and will be removed from `wrangler.toml` routes once the master portal is verified.

## Payments · what we accept

| Method | Currency | Use |
|--------|----------|-----|
| Wompi | COP | All clients · bank transfers (PSE) + cards |

**No Stripe account.** We do NOT use Stripe for our own payments. We CAN integrate Stripe into client sites (they provide their own merchant account). Don't list Stripe as a PymeWebPro payment method on any site.

**No Wise / CAD / USD.** Even though Mike has a Wise Business account personally, PymeWebPro the studio does NOT bill in CAD or USD and does NOT use Wise as a payment method. Colombian customers pay in COP via Wompi. Period.
→ Setup details: `memory/context/studio.md`

## Positioning · key phrases

- "Estudio de diseño web en Medellín. Construido con Claude de Anthropic."
- "Páginas de ventas custom. Sub-1s LCP. Entrega en 48 horas."
- "Claude construye. Mike y Santiago supervisan, revisan, lanzan."
- "Lo que las agencias cobran $5M COP, nosotros lo cobramos $390k COP."

**What we don't claim:**
- **Updated 2026-05-20 per Mike:** "Canadian-owned" IS allowed as a subtle trust signal (and London, Ontario can be listed as "the Canadian studio" beside the Medellín studio). Do NOT lead with it and do NOT imply superiority over Colombians. The market is still Colombian SMBs only, billed in COP. Mike being Canadian and a leader are facts; Santiago is a paisa partner.
- We don't position ourselves as senior engineers, a development team, or programmers. The pitch is honest: a small studio that uses Anthropic AI to do the technical work, with humans setting briefs and reviewing output before shipping.
- We don't serve NA / US / Canada / international clients. Customers are Colombian SMBs only.

## Mike's preferences

- Keep responses brief and to the point
- Use contractions in English copy ("we're", "it's", not "we are", "it is")
- No emojis unless explicitly asked
- Show real numbers (Lighthouse 100, LCP 0.84s) instead of vague claims
- Honest framing over marketing-speak (e.g., "Why we cost less" sections)
- Prefer files over chat for durable artifacts

## Hard writing rules (apply to chat AND every site we ship)

1. **No em dashes.** Never use the em dash character (the long dash). Use commas, periods, colons, parentheses, or " · " separators instead. This applies to all client sites, all marketing copy, all chat responses. The em dash is a tell of AI writing and Mike does not want it in anything we ship.
2. **No "delve", "tapestry", "navigate" (when used metaphorically), "in the realm of", "embark on a journey", "a testament to", "stands as", "fast-paced world", "in today's", "leverage" (as a verb), "vibrant ecosystem".** Replace with plain language.
3. **Sentence case headings** unless a brand specifically uses Title Case (none currently do).
4. **Use contractions** in English copy.

## Hard build constraints (apply to every HTML file we ship)

These are non-negotiable. If a brief asks for something that violates these, push back.

1. **No runtime CSS frameworks via CDN.** Never use `<script src="https://cdn.tailwindcss.com">` or any equivalent. That CDN is for prototyping only and adds a 3MB+ JS payload that destroys LCP. We ship custom inline `<style>` blocks (AI-built, human-reviewed; no framework runtime). Tailwind, Bootstrap, Bulma, Pico · none belong in production output.
2. **No CDN scripts at all** except: Google Fonts (preconnect + stylesheet), Pexels images (CDN URLs OK), and explicitly-allowed libraries we choose case-by-case (e.g., a small Chart.js or three.js when the build genuinely needs it).
3. **Real product photos required.** A fashion site without product photos is wireframe-tier. A real-estate site without property shots is wireframe-tier. Use Pexels CDN URLs at minimum, base64-embed when local files are provided. CSS gradients are NEVER an acceptable substitute for images on product, portfolio, or gallery cards.
4. **No marquee scrolling text bars.** Dated 2018 e-comm trope. If you need to convey shipping/return/promo info, use a static notice bar.
5. **No fake placeholder phone numbers** like `+57 300 000 0000`. Either use the real number, or omit the number, or write `[client phone]` as an obvious placeholder.
6. **Footer must include:**
   - For Colombian-facing sites: NIT, registered address, Cámara de Comercio compliance line
   - For all sites: "Sitio web por PymeWebPro" (Spanish) or "Built by PymeWebPro" (English) credit, linking to https://pymewebpro.com
7. **Sub-1s LCP is a baseline, not a stretch goal.** Critical CSS inlined, fonts preconnected, images width/height locked, lazy-loading below the fold, no render-blocking JS.
8. **Self-contained single-file HTML.** All CSS in `<style>`, all JS in `<script>`. The mockup pipeline wraps each file as a JS template literal · external dependencies break this.
9. **Lighthouse 100 across Performance, Accessibility, Best Practices, SEO** at launch. If any score drops below 95, fix before shipping.
10. **Schema.org JSON-LD baseline.** Every site ships with at minimum: Organization, WebSite, Service (one per pricing tier), and FAQPage blocks. Inline `<script type="application/ld+json">` in head. Any inline JSON-LD must have its SHA-256 hash added to the CSP allowlist in `_headers` for sites served by Cloudflare Pages.
11. **Security headers baseline (Cloudflare Pages sites).** `_headers` must include: `Strict-Transport-Security: max-age=63072000; includeSubDomains; preload`, `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, a `Permissions-Policy` zeroing out unused features, and a `Content-Security-Policy` with hash-locked inline scripts. Goal is qualys.ssllabs.com A+ on day one.

### Pre-deploy validation

Run `npm run check` from `portal/`. The check pipeline includes:
- `check:worker` · every JS file in src/ parses cleanly
- `check:spa` · the Babel-transpiled SPA template literal parses cleanly
- `check:standards` · every `manual-mockups/<slug>/index.html` validates against the build constraints above

Any failure blocks deploy. Don't bypass with `--no-verify` · fix the underlying issue.

## Brand voice rules

These apply to all copy on every site we ship.

- **Restrained over edgy.** Daga's "quiet confidence" beats "Vestida para arruinarles la noche." We don't sell trying-too-hard.
- **Honest over aspirational.** "Why we cost less" beats "disrupting the industry." Real numbers (LCP 0.84s) beat vague claims (super fast).
- **Specific over generic.** "Cloudflare Workers · 330+ edge locations" beats "enterprise-grade infrastructure."
- **Confident over hedged.** "We don't ship sites we're not proud of" beats "we strive to deliver excellence."

## Common AI mistakes that show up when sessions skip the memory

If you find yourself reaching for any of these patterns, stop and reconsider:

- Tailwind CDN (see build constraint #1)
- CSS gradients in place of product photos (see build constraint #3)
- Marquee bars (see build constraint #4)
- Em dashes (see writing rule #1)
- "Marketplace" claims with no marketplace mechanics
- Edgy product naming for sensual/luxury brands ("Veneno", "Medusa", "Latigazo" · leans tasteless)
- Fake addresses and phone numbers
- Generic stock copy like "made with passion in Medellín"
- Skipping favicon, OG image, schema.org JSON-LD
- Skipping the NIT footer block on Colombian sites

When in doubt, look at how Daga, BWI, or Espacio Dental handle the equivalent moment. Those are the reference quality bar.

## Glossary highlights

| Term | Meaning |
|------|---------|
| **manual mockup** | Self-contained HTML site served from `mockups.pymewebpro.com/<slug>/` |
| **the worker** | Cloudflare Workers code at `~/code/pymewebpro/portal/` |
| **the SPA** | React admin in `src/index.js` (Babel standalone) |
| **Lanzar final** | Deploy customer site to production |
| **wizard intake** | The step-by-step client onboarding flow |
| **La página de ventas** | The single product · $390.000 COP one-time + à la carte add-ons, Colombia-only |
| **mockups subdomain** | mockups.pymewebpro.com |
→ Full glossary: `memory/glossary.md`

## Recent changes

- **2026-05-24 (later x42)** · **Plan de ventas chatbot reworded to the deliverable reality (Mike): website bot that pushes to WhatsApp via link, NOT a Cloud-API bot.** A true "chatbot on WhatsApp" needs WhatsApp Cloud API + per-client Meta business verification + a dedicated number (the same wall Catalina hits) + per-conversation cost, so promising it with "no setup fee" overpromised. Mike's call: blue-collar owners don't need the nuance, so the website chatbot answers questions and hands the customer to their normal WhatsApp with one tap (wa.me), which is deliverable today with no setup fee. Reworded in 4 places: `index.html` + `en/index.html` (Plan de ventas card), `portal/src/proposal-generator.js` (PROPOSAL_TEXT_ES/EN hosting1), `portal/src/ventas-guia.html` (plan callout). ALSO fixed stale client-facing text: the proposal FEATURES_ES/EN add-on line still advertised dropped add-ons (página adicional, GBP, GA4, CRM, chatbot) — now reads "Adicionales à la carte ($75.000 c/u): catálogo en la página, catálogo o menú descargable en PDF, versión bilingüe". Also removed the now-dead `chatbotMonthly` (computePricing return + template render) and `chatbotNote` (ES/EN text keys) code that was left over from when the chatbot was a paid add-on. Checks: proposal `node --check` pass; em-dash 0; CSP in sync; `check:standards` 0 fails. **Deploy:** site `git push`; worker `cd portal && wrangler deploy`.
- **2026-05-24 (later x41)** · **Rename the "PDF button" add-on to the deliverable + fix stale cockpit prices (Mike: "$75k for a button?").** "Botón de catálogo PDF" / "PDF catalog button" read like paying 75k for a button, so renamed it after what the client actually gets: **"Catálogo o menú descargable (PDF)" / "Downloadable catalog or menu (PDF)"** (the internal key stays `pdf`). Updated in `index.html` + `en/index.html` (add-on rows), `portal/src/proposal-generator.js` (ADDON_CATALOG pdf label ES+EN), `portal/src/ventas-guia.html` (two prose mentions, now "catálogo en la página" vs "catálogo o menú descargable en PDF" so the two catalog add-ons are distinguishable). ALSO fixed a leftover bug: `portal/src/ventas-midia.html` cockpit `var ADDONS` still displayed the OLD prices (catalog $180.000, pdf $50.000) even though proposal pricing was already 75k from x39, so the proposal popup showed wrong figures; now all three show $75.000. `frontend.html` "Texto del botón PDF / PDF button label" left as-is (that's the client onboarding field where a paying client labels their own download button, not the sales menu). Checks: proposal-generator + ventas-midia inline JS `node --check` pass; em-dash scan 0; CSP in sync (text-only edits); `check:standards` 0 fails. **Deploy:** site `git push`; worker `cd portal && wrangler deploy`.
- **2026-05-24 (later x40)** · **Homepage "La prueba": two linked Google reviews (Inviersol + The Schedulator) + pricing spacing fix (Mike).** The reskinned homepage only carried a paraphrased Inviersol line with no link. Replaced it with a 2-up `.proofs` grid of real testimonials, each with the actual quote, attribution, and a "Ver en Google / See on Google →" link to the PymeWebPro Google Business place page (`https://www.google.com/maps/place/PymeWebPro/...16s%2Fg%2F11nhznzt0l`, the shared review URL recovered from the pre-reskin homepage git history `c305b6d~1`). Inviersol = Santiago Florez ("Muy recomendado... súper rápido"); The Schedulator = Patrick Detzner (PE-teacher-built SaaS scheduling tool for teachers, faithful condensed quote, ES carries "Traducido del original en inglés"). Added `.proofs` grid CSS (2-col, 1-col under 760px), `.proof .gsrc` link (color + bottom-border + "→" so it's not colour-only, colour-blind safe) and `.proof .tnote`. Also bumped the two pricing sub-headings ("Planes mensuales" / "Adicionales") top margin `1.5rem`→`2.8rem` so they stop touching the price box and the plan cards. Files: `index.html` + `en/index.html` (EN Schedulator quote kept in original English, no translation note). CSP still in sync (inline-style-attr + `<style>` edits, refresh script reports 63/16 match); em-dash scan 0; `check:standards` 0 fails (ES + EN ✓, 23KB each). **Deploy:** site `git push` (Pages auto). No worker change.
- **2026-05-24 (later x39)** · **Two-tier MRR ladder + flat $75k add-ons; chatbot moved out of add-ons into the monthly plan (Mike decisions).** Recurring is now a two-tier ladder on top of "hosting solo $30k/mes": **Plan de presencia $59.000/mes** (hosting + Ficha de Google activa + hasta 2 cambios + reporte) and **Plan de ventas $150.000/mes** (todo lo anterior + asistente de ventas 24/7 = el chatbot que capta/responde/agenda en página y WhatsApp, sin costo de instalación). Chatbot is NO LONGER a one-time add-on. The 3 remaining add-ons are now flat **$75.000 c/u** (Catálogo, Botón PDF, Bilingüe). Files: `portal/src/proposal-generator.js` (ADDON_CATALOG catalog 180k→75k, pdf 50k→75k, bilingual 75k; PROPOSAL_TEXT_ES.hosting1 + PROPOSAL_TEXT_EN.hosting1 now present the 3 monthly options incl. Plan de ventas), `index.html` + `en/index.html` (add-on rows all $75.000; both already carry the two .plans cards $59k/$150k from x37/x38), `portal/src/ventas-guia.html` (proposal step parenthetical drops chatbot + notes $75k each; new "Después de ganar: ofrécele un plan mensual" callout with the 3-tier ladder; Reglas de oro add-on line updated). `portal/src/ventas-midia.html` cockpit ADDONS already = [catalog, pdf, bilingual] from x38 (no chatbot). Checks: proposal-generator + outreach `node --check` pass; em-dash scan clean (0); CSP hashes refreshed (63 scripts / 16 styles in sync, this is what unblocked Mike's failed push); `check:standards` 0 fails (pymewebpro ES + EN ✓). **Deploy:** site `git push`; worker `cd portal && wrangler deploy`.
- **2026-05-24 (later x38)** · **Add-on lineup trimmed + GBP folded into the base (Mike decision).** Add-on menu is now just 4: Catálogo $180k, Botón PDF $50k, Bilingüe $75k, Chatbot $190k+$80k/mes. Dropped: Ficha de Google (GBP), Analítica avanzada, Copywriting, CRM, Página adicional. GBP SETUP is now included in the $390.000 base (the new positioning is "página + que te encuentren en Google"); GBP ongoing management stays in the Plan de presencia $59k/mes. Edited in 5 places: `index.html` + `en/index.html` (add-on lists), `portal/src/proposal-generator.js` (ADDON_CATALOG trimmed to 4; added "Ficha de Google configurada" / "Google Business Profile set up" to FEATURES_ES/EN so the base proposal shows it), `portal/src/ventas-midia.html` (cockpit ADDONS array), `portal/src/ventas-guia.html` (proposal step copy). Checks: proposal-generator + ventas-midia node --check pass; both homepages pass check:standards (0 fails); CSP hashes refreshed; em-dash scan clean. **Deploy:** site `git push`; worker `cd portal && wrangler deploy`.
- **2026-05-24 (later x37)** · **pymewebpro.com homepage rebuilt: blue-collar reskin (Mike: old site too flashy for the INVIERSOL-type ICP).** `index.html` ES rewritten from the dark editorial "Capítulo" magazine design to a light, warm, plain, proof-led page (92KB -> 21KB). New: honest positioning band ("Tú traes la gente, nosotros hacemos que te elijan"), "para quién" (vendors/trades/instaladores), "qué recibes" with GBP, INVIERSOL real 5-star review as proof, clear price + the à-la-carte add-on menu (static list, dropped the interactive builder) + Plan de presencia $59k/mes (includes hosting), "cómo empezar". PRESERVED: full SEO head (schema Organization/WebSite/Service/FAQPage incl. the on-message "no garantizamos leads" answer), OG/twitter, hreflang, favicon, async fonts, the Valentina widget (`data-valentina-open` primary CTA + WhatsApp secondary), language switch, legal + guides links. Fixed banned word "líderes" in title/OG/footer. Ran `scripts/refresh-csp-hashes.py` (65 scripts / 17 styles in sync). `check:standards` passes (0 fails; footer-brand-legibility rule satisfied via `footer.site .brand{color:#FFFFFF}`). EN twin (`en/index.html`) rebuilt to match (same design/style/wiring, English copy, FAQ schema in EN, hreflang/canonical for /en/, lang-switch EN active). Both pages pass `check:standards` (0 fails, 21KB each); CSP hashes refreshed (63 scripts / 16 styles in sync). **Deploy:** `cd ~/code/pymewebpro && git add -A && git commit -m "..." && git push`.
- **2026-05-24 (later x36)** · **Stop logging unsent drafts + let Santi delete history items (Mike: 4 "Borrador / nota" on a lead he never messaged).** Each "Generar mensaje" click was logging a 'note' activity, so generating several drafts cluttered the history and looked like sends. `portal/src/outreach.js`: the `draft` endpoint no longer inserts a 'note' activity (only `log-send` records anything; the mockup 'Listo: mockup' draft in cockpit.js is separate and kept). `portal/src/ventas-midia.html`: each Historial item in the detail modal now has a × that DELETEs the activity (`/api/admin/crm/activities/<id>`, backend already supports it) and removes it from the timeline. One-time cleanup of existing draft notes: `wrangler d1 execute pymewebpro-portal --remote --command "DELETE FROM activities WHERE subject LIKE 'Draft generated%';"`. `node --check` passes, em-dash scan clean. **Deploy:** `cd portal && wrangler deploy`.
- **2026-05-24 (later x35)** · **Outreach prompts: add the "why a sales page matters" value line (Mike).** `portal/src/outreach.js`: all 4 prompts now include "Recalca por qué importa: una página de ventas convierte las visitas en clientes guiándolos a UNA acción (reservar/escribir/comprar), no es una vitrina..." so drafts make the case for having a marketing/sales page that drives leads to a sale, not just the existence of a website. Pairs with the now-required base price and the no-notes rule from x34. `node --check` passes, em-dash scan clean. **Deploy:** `cd portal && wrangler deploy`.
- **2026-05-24 (later x34)** · **Outreach prompts: always state base price, drop the "Notas internas" block (Mike).** `portal/src/outreach.js`: (1) all 4 prompts now REQUIRE mentioning the base price naturally ("menciona SIEMPRE el precio base, desde $390.000 COP, pago único, IVA incluido; adicionales aparte"), replacing the optional "si mencionas precio" phrasing. (2) The 2 WhatsApp prompts now end with "Devuelve SOLO el texto del mensaje ... sin notas internas, sin explicaciones, sin '---'", because the model had been appending a rationale block after the message (email prompts already returned JSON-only). `node --check` passes, em-dash scan clean. **Deploy:** `cd portal && wrangler deploy`.
- **2026-05-24 (later x33)** · **Outreach prompts: product-facts guardrail (Mike caught a draft promising "un año de hosting").** The model invented a free year of hosting because the prompts stated the price but never the inclusions. `portal/src/outreach.js`: added a "HECHOS DEL PRODUCTO (no inventes inclusiones)" line to all 4 system prompts (Santi/Mike, WhatsApp/email): $390.000 COP one-time IVA incluido, includes 1 MONTH hosting+support (NOT a year), ~48h delivery, 2 revisions, and an explicit ban on promising a year of hosting, free hosting/domain, or any inclusion not listed. Note: the cockpit "Generar mensaje" always uses tone=santi (the pasted draft that said "soy Mike" came from the Mike-voice path). `node --check` passes, em-dash scan clean. **Deploy:** `cd portal && wrangler deploy`.
- **2026-05-24 (later x32)** · **Close loop wired: "Cobrar depósito" in the cockpit + deposit = Ganado (Mike: "client likes the proposal, then what?").** The 30/70 Wompi machinery existed but wasn't reachable from Mi día. Now: (1) `proposal-generator.js` exports `dealTotalCop(deal)` (base + add-ons, legacy pro aware); `deposit-links.js` uses it so the deposit/balance links charge 30%/70% of the REAL proposal total (was 30% of base only, undercharging when add-ons were present); the deals SELECT now includes `addons`. (2) `deposit-links.js` `processDepositPayment` now flips the lead to `status='converted'` (plus sales_qualified) so a paid deposit auto-moves the card to Cerrados/Ganado in the cockpit. (3) `ventas-midia.html`: "Cobrar depósito" button on Por-cerrar cards -> calls `/api/admin/deals/<dealId>/deposit-link` -> composes a WhatsApp message with the Wompi link + COP amount (new `formatCop` helper, `chargeDeposit`). (4) Santi's guide updated with the deposit step. Requires `WOMPI_PUBLIC_KEY` + `WOMPI_INTEGRITY` on the worker. `npm run check` passes (0 fails), em-dash scan clean. **Deploy:** `cd portal && wrangler deploy` (after migration 0011 so deals.addons exists).
- **2026-05-24 (later x31)** · **Mockup image gen: try multiple Gemini models + surface the real error (Mike: "no images in the latest generate", silent fallback to gradient).** `demo-img.js` `genDemoImg` now loops candidate image models (`gemini-2.5-flash-image-preview`, `gemini-2.0-flash-preview-image-generation`, `gemini-2.0-flash-exp-image-generation`) and returns a detailed joined error if all fail (so we can see 404/403/no-image/key-missing). `ventas-midia.html` Imágenes panel shows that error inline (red) under each image instead of a transient toast. Diagnostics so we can tell whether it's the GEMINI_API_KEY secret missing vs the key lacking image-gen access. `node --check` passes, em-dash scan clean. **Deploy:** `cd portal && wrangler deploy` (ensure `wrangler secret put GEMINI_API_KEY` was run first).
- **2026-05-24 (later x30)** · **Mockup-view tracking: stop counting Santi's own previews (Mike: banner said 4 viewed but only 1 mockup exists).** Every lead has a demo URL (generic fallback), and the beacon logged a view on ANY open, so previewing demo links from the cockpit inflated the "vieron tu mockup" count with false positives. Fix: the beacon in all 4 demo templates now skips logging when the URL has `?preview=1`; the cockpit detail "Ver mockup" link opens with `?preview=1` (Santi preview, not counted), while the "Copiar link" share link and the link inside the sent WhatsApp message have no flag (real prospect opens still count). One-time data cleanup to reset existing false views: `wrangler d1 execute pymewebpro-portal --remote --command "DELETE FROM activities WHERE subject='Mockup abierto';"`. `node --check` passes, em-dash scan clean. **Deploy:** `cd portal && wrangler deploy`.
- **2026-05-24 (later x29)** · **Mi día: viewed-mockup cards now visibly highlighted blue (Mike: banner said "4 vieron tu mockup" but no way to see which).** `portal/src/ventas-midia.html`: leads with a 'Mockup abierto' activity (active) now render as a BLUE card (`.card.viewed`, #1f4e79 / #cfe0f5) with a "Vio el mockup" chip, in any column, and sort to the top (`byHot` = viewed first, then alert, then score). Blue is distinct from the red "needs action" cards (good for Mike's red-green colorblindness) and paired with the text label so it never relies on color alone. Removed the now-duplicate red 'Vio el mockup' signal from `alertFor('mockup')`. `node --check` passes, em-dash scan clean. **Deploy:** `cd portal && wrangler deploy`.
- **2026-05-24 (later x28)** · **Mi día: "Agendar llamada" now sends the booking link to the prospect (Mike: it was Santi scheduling with himself).** `portal/src/ventas-midia.html`: `bookCall` no longer opens the calendar for Santi; it opens the WhatsApp compose panel with a short message + the booking link so the PROSPECT picks a time (review -> send -> logged, same flow as other messages). Still pending: `CAL_LINK` is the `cal.com/medellinguide/intro` placeholder and needs Santi's real PymeWebPro booking link. `node --check` passes, em-dash scan clean. **Deploy:** `cd portal && wrangler deploy`.
- **2026-05-24 (later x27)** · **Mi día: generation is stage-triggered, not per-card buttons (Mike: every card could generate a mockup/proposal, wrong).** `portal/src/ventas-midia.html`: removed the always-present "Generar mockup"/"Armar propuesta" buttons from the detail panel. Now MOVING a card into a stage opens a setup popup that asks for details, then generates: `moveTo('mockup')` opens new `openMockupSetup` (pick local/English variant -> generate example + WhatsApp message via prepareMockup); `moveTo('proposal')` opens `showProposalPanel` (add-ons + notes -> generate). The detail panel is now stage-aware: Generar mensaje + Agendar llamada always; "Enviar mockup" only when in Mockup; "Editar propuesta"/"Ver propuesta" only when in Propuesta; mockup link + language toggle only once the card has reached Mockup. `node --check` passes, em-dash scan clean. **Deploy:** `cd portal && wrangler deploy`.
- **2026-05-24 (later x26)** · **Mi día: "Agendar llamada" reliability fix (Mike: button doesn't work).** `portal/src/ventas-midia.html`: `bookCall` now opens the calendar before the modal closes (fires inside the user gesture) and, if the popup is blocked, copies the link to the clipboard with a toast instead of doing nothing. Open caveat flagged to Mike: `CAL_LINK` is still the placeholder `cal.com/medellinguide/intro` (Medellín Guide, not a PymeWebPro/Santi calendar) and needs swapping; also offered to change the action to send the booking link to the prospect by WhatsApp instead of opening it. `node --check` passes, em-dash scan clean. **Deploy:** `cd portal && wrangler deploy`.
- **2026-05-24 (later x25)** · **Phase 2c (Proposal column) + Gemini mockup imagery + "Generar mockup" button.** THREE things. (1) PROPOSAL: `proposal-generator.js` now prices add-ons (new `ADDON_CATALOG` + `computePricing` + `fmtCOP`, base $390.000 + selected add-ons, 30/70 split, COP), reads the selection + requirement notes from the deal (`buildBrief`), and renders priced line items + a "Lo que pediste" block in the printable proposal; `generateProposal` is now exported. New migration `0011_deal_addons.sql` (deals.addons TEXT). `cockpit.js` `POST /api/admin/cockpit/proposal` creates/links a deal from the lead with the add-ons+notes, advances the lead to `lead_stage='proposal'`, and calls the generator. `ventas-midia.html` has a "Armar propuesta" builder (add-on checkboxes + notes) in the detail panel; the card flags red "Listo para enviar" when `deal.proposal_status==='ready'` (deals now loaded into `DEALS_BY_LEAD` from the grid), with a "Ver propuesta" link to `/admin/proposal/<dealId>`. (2) MOCKUP IMAGES via Gemini: new `demo-img.js` (`serveDemoImg` public-serve-from-R2-or-gradient, `genDemoImg` admin-only). A small SHARED set of 5 images (dental-hero/smile/braces, generic-hero/team) is generated ONLY by an admin action (cost control: never auto-generated on a public view, so crawlers/prospects can't trigger paid gen or lock bad images), cached in R2 `ASSETS` forever, reused across every mockup. `GET /demo-img/<key>` route in index.js; templates reference `/demo-img/<key>` with a gradient overlay (so they look fine before images are generated). New "Imágenes" panel in Mi día to generate/review/regenerate each. Dental before/after now shows braces -> straight teeth. Requires `GEMINI_API_KEY` secret on the worker. (3) "Generar mockup" button added to the detail panel (initiate the AI mockup message from any card, not just on drag). Also reverted the earlier random-stock-photo experiment (picsum) which looked terrible ("rocks and a lake on a dental page"). `npm run check` passes (0 fails); all worker JS node --check OK; em-dash scan clean. **Deploy:** `cd portal && wrangler secret put GEMINI_API_KEY` (paste key), then `wrangler d1 execute pymewebpro-portal --remote --file=migrations/0010_demo_lang.sql && wrangler d1 execute pymewebpro-portal --remote --file=migrations/0011_deal_addons.sql && wrangler deploy`. Then open Mi día -> Imágenes and generate the 5 images.
- **2026-05-24 (later x24)** · **Mockup polish: variant (local/English), placeholder images, sales-funnel section (Mike feedback).** (A) VARIANT: mockups now come in 4 combos (dental/generic × es/en). New `demo_lang` column on leads (migration `0010_demo_lang.sql`, SEPARATE from `language` which stays Spanish for owner comms) drives it; default/null = local Spanish (fixes "too gringo"), 'en' = foreigner-focused. `index.js` `pickDemoTemplate(cat, lang)` + serveDemo read `demo_lang`; new files `demo-dental-es.html` (local Spanish dental) and `demo-generic-en.html` (English generic) join the existing `demo-dental.html` (English) and `demo-generic.html` (Spanish). `crm.js` adds `demo_lang` to EDITABLE_COLUMNS.leads + the grid SELECT. `ventas-midia.html` detail panel has a "Versión: Local / Inglés" toggle that PATCHes `demo_lang`. (B) IMAGES: all 4 templates now use seeded placeholder photos (picsum, grayscale + brand-tint overlay) on the hero, an image band, and (dental) before/after, captioned "tus fotos aquí". (C) FUNNEL SECTION: every template has a "No es solo una página, es un embudo de ventas" / "Not just a page, a sales funnel" 3-step section (Llega -> Se interesa -> Te escribe). `npm run check` passes (worker+SPA+standards, 0 fails); cockpit.js node --check OK; em-dash scan clean; all 4 templates carry NEGOCIO/CIUDAD/LEAD_ID. **Deploy (migration first):** `cd portal && wrangler d1 execute pymewebpro-portal --remote --file=migrations/0010_demo_lang.sql && wrangler deploy`.
- **2026-05-24 (later x23)** · **Mockup message: continuation tone, no cold re-intro (Mike feedback).** By the Mockup stage Santi is already in an active WhatsApp chat, so the "Hola, soy Santi de PymeWebPro" opener was wrong. `portal/src/cockpit.js`: the mockup-msg system prompt now states it is an ongoing conversation and forbids self-introduction; fallback rewritten to "Acá te dejo el ejemplo que te comenté...". `portal/src/ventas-midia.html`: sendMockup fallback text matched to the same continuation tone. `node --check` passes on both, em-dash scan clean. **Deploy:** `cd portal && wrangler deploy`.
- **2026-05-24 (later x22)** · **Mi día: manually-added leads were invisible (Mike: added "Clinica Didi", couldn't find it).** `portal/src/ventas-midia.html`: "Para contactar" and "Por conseguir número" sorted by score DESC, so a manual add (score 0/null) sank to the bottom under all the scored seed leads. Now both sort newest-first (`byNew` = created_at DESC, score as tiebreaker) so new adds surface at the top. Also the add confirmation toast now names the destination column ("Agregado a Para contactar" vs "Por conseguir número") based on whether a WhatsApp was entered. `node --check` passes, em-dash scan clean. **Deploy:** `cd portal && wrangler deploy`.
- **2026-05-24 (later x21)** · **Mi día: search bar + full mockup link in detail (Mike feedback).** `portal/src/ventas-midia.html`: (1) added a search input in the topbar that live-filters every kanban column (`matchSearch` over business_name/name/category/city/email/instagram/whatsapp/phone/current_site; render uses a filtered `L`). (2) The detail panel now shows the full clickable mockup link (`DEMO_BASE/demo/<id>`, opens in a new tab, wraps fully so it is not truncated) plus a "Copiar link" button, so Santi can preview the mockup and grab the URL. `node --check` passes, em-dash scan clean. **Deploy:** `cd portal && wrangler deploy`.
- **2026-05-24 (later x20)** · **Phase 2a/2b: Mockup column auto-generation (every business type + AI message that self-drafts).** (1) New `portal/src/demo-generic.html`: a clean, sector-neutral Spanish one-pager teaser (same PWP preview banner, bilingual "no usamos plantillas" block, and `/api/demo/<id>/seen` open-tracking beacon as the dental one). Wired in `index.js`: imported, added to `DEMO_TEMPLATES.generic`, and `pickDemoTemplate` now falls back to generic for any non-dental category, so `mockups.pymewebpro.com/demo/<lead-id>` works for ALL leads. (2) New `portal/src/cockpit.js` (`handleCockpitRoutes`, wired before the /api/admin/* catch-all): `POST /api/admin/cockpit/mockup-msg` AI-drafts the accompanying WhatsApp message (Claude Haiku, Santi voice, tú, no marketing-speak, no em dashes), appends the demo URL, and stores it as a `Listo: mockup` activity (done=0). (3) `ventas-midia.html`: dragging a card into Mockup now auto-calls that endpoint (`prepareMockup`), the card flips to red "Listo para enviar" once the draft exists (`mockupReady` = draft newer than last_touched_at), and "Enviar mockup" uses the AI draft. `hasDemo` now always true. Also fixed: nextStep uses isWon/isLost (not stale status); removed banned word "World-class" from demo-dental. Requires `ANTHROPIC_API_KEY` on the worker (already set for the proposal generator). `npm run check` passes (worker+SPA+standards, 0 fails), em-dash scan clean. **Deploy:** `cd portal && wrangler deploy`. **Phase 2c/2d still to build:** proposal add-on form + generator extension, and the to-close stale nudge.
- **2026-05-24 (later x19)** · **Mi día: attention chip replaced by red card background (Mike request).** `portal/src/ventas-midia.html`: removed the per-card "⚠ Sigue ahora / Vio el mockup / Sin respuesta" chip and the "esperando" wait chip. Cards needing action now use a medium-red background (`.card.alert{background:#E08A7A}`) with a small ⚠ glyph next to the name as the non-color cue (Mike is red-green colorblind, so color-only would break his own standing rule; flagged to him, removable on request). Alert-card text darkened to var(--ink) for contrast on red. Cerrados keeps the ✓ Ganado / ✕ Perdido chips. `node --check` passes, em-dash scan clean. **Deploy:** `cd portal && wrangler deploy`.
- **2026-05-24 (later x18)** · **Mi día card fix: company names were invisible (Mike screenshot).** Not a data bug, the names were present. `portal/src/ventas-midia.html`: the card header was a flex row (`.cardtop`) sharing space between the name and the "⚠ Sigue ahora" chip; in the narrow 6-up columns the chip squeezed the name to a 1-char-per-line vertical sliver that read as blank. Fix: chip now sits on its own line (`.cardchip`), the business name spans the full card width on its own line, and the overly aggressive `overflow-wrap:anywhere`/`word-break:break-word` (which broke mid-word) was softened to `overflow-wrap:break-word`. Also note: Mike manually moved the bogus `sales_qualified+converted` test leads back to Seguimiento, so the false "Ganado" count is handled (the underlying stale `status='converted'` can still be cleaned later if desired). `node --check` passes, em-dash scan clean. **Deploy:** `cd portal && wrangler deploy`.
- **2026-05-24 (later x17)** · **Mi día: stage mapping now follows lead_stage (source of truth), + robust card names (Mike feedback).** `portal/src/ventas-midia.html`: (1) the board was treating legacy `status='converted'` as Won, which dumped ~10 real `marketing_qualified` working leads into Cerrados. Now Won = `lead_stage==='sales_qualified' && status==='converted'` only; everything else maps purely by `lead_stage` exactly like the CRM funnel (`marketing_qualified` and `contacted` -> Seguimiento, `mockup`/`proposal`/`sales_qualified` to their columns, `disqualified` -> Cerrados/Perdido). So those MQ leads now correctly sit in Seguimiento, not Cerrados. (2) Added `leadName(l)` fallback (business_name -> name -> @instagram -> email -> site domain -> "Sin nombre") used on cards, the detail modal, and the no-number lane, because many legacy form/checkout leads have a null `business_name` and were rendering blank. `node --check` passes, em-dash scan clean. **Deploy:** `cd portal && wrangler deploy`. (Separately: those MQ leads carrying a stale `status='converted'` are harmless now but Mike may still want a one-time cleanup to reset that field.)
- **2026-05-24 (later x16)** · **Mi día kanban layout polish (Mike live feedback).** `portal/src/ventas-midia.html`: (1) board now uses six equal flex columns (`flex:1 1 0; min-width:0`) so all stages are visible without horizontal scroll, page widened to 1680px; (2) cards hard-wrap text (`overflow-wrap:anywhere`, `overflow:hidden`) so nothing bleeds into the next column; (3) cards compressed to a summary (business name + status chip + one mini line: next step + touch count, or outcome in Cerrados) and the WHOLE card opens the detail panel on click; all per-card action buttons moved into that detail modal (Generar mensaje, Enviar mockup, Agendar llamada, Mover a, Ganado, Perdido). Note for Mike: the Cerrados column counts (e.g. 10 Ganados / 9 Perdidos) come from pre-existing leads with `status='converted'` or `lead_stage='disqualified'` (likely Wompi-checkout test rows), now visible because the old 3-col view had no Cerrados column. `node --check` on extracted script passes, em-dash scan clean. **Deploy:** `cd portal && wrangler deploy`.
- **2026-05-24 (later x15)** · **Mi día rebuilt as a 6-column drag-and-drop kanban (Phase 1 of the funnel cockpit).** Per Mike's design sessions (flowchart + kanban mockups at `~/code/pymewebpro-funnel-flow.html` and `~/code/pymewebpro-funnel-kanban.html`), `portal/src/ventas-midia.html` went from 3 columns to the full pipeline: **Para contactar → Seguimiento → Mockup → Propuesta → Por cerrar → Cerrados**, plus the "Por conseguir número" lane. Cards are draggable (HTML5 DnD); dropping into a column PATCHes `lead_stage` via `/api/admin/crm/leads/<id>` (`contacted`/`mockup`/`proposal`/`sales_qualified`, or `disqualified` when dropped in Cerrados = Lost). Won stays an explicit action (status=converted). Sending the first message now auto-advances Para contactar → Seguimiento (PATCH lead_stage=contacted after log-send), which fixes the "card vanishes after I message them" gap. Attention is colorblind-safe: red cards carry a ⚠ chip + text label (Sigue ahora / Vio el mockup / Sin respuesta insiste), never color alone; a detail-modal "Mover a:" row is the touch fallback for drag. New stage values `mockup` + `proposal` need no migration (server `updateRow` stores any lead_stage text; no enum check). `npm run check` passes (worker + SPA + standards, 0 fails), em-dash scan clean. **Phase 2 still to build:** the three auto-generations (mockup message on entry to Mockup, proposal on add-on form submit reusing `proposal-generator.js`, To-close stale nudge) so cards turn red "listo para enviar" on their own. **Deploy:** `cd portal && wrangler deploy`.
- **2026-05-24 (later x14)** · **QA pass on the Mi día funnel + CRM nav fix (Mike: "once you open the crm, you cant get back to mi dia").** Ran two QA agents over the cockpit/demo code, then fixed: (1) NAV · `crm.js` crmPageHTML header now has a "← Mi día" link to `/` (was stranded with only Recursos + admin links). (2) MAJOR · `ventas-midia.html` `isWarm()` gained `&& l.status!=='converted'`, so "Ganado" leads now leave the "Por cerrar" column instead of sticking (won sets lead_stage=sales_qualified + status=converted). (3) `isNew()` now also includes legacy `lead_stage==='raw'` leads (were invisible in every column). (4) Detail-modal "Agendar llamada" now closes the modal before opening Cal. (5) `index.js` `logDemoView` dedup key now keys on the Medellín calendar day (UTC-5), not UTC, so an evening mockup view doesn't double-log across the UTC midnight boundary. (6) `crm.js` `showAiDraftPanel` send button no longer dead on no-number leads (copies the message instead, mirroring Mi día). Demo engine re-verified live by QA (mockups.pymewebpro.com/demo/<lead-id> → 200, name substituted, beacon fires, activity subject 'Mockup abierto' matches the badge). `npm run check` passes (0 fails), em-dash scan clean. **Deploy:** `cd portal && wrangler deploy`.
- **2026-05-24 (later x13)** · **Mi día funnel cockpit, STEP 1 (surface existing data).** `ventas-midia.html`: funnel overview strip (Sin contactar → En conversación → Interesados → Ganados → Perdidos, live counts from lead_stage/status/last_touched); per-card touch line + next step (`touchLine`/`nextStep` from touches_count/last_touched_at/lead_stage/whatsapp); "Ver" detail modal (`openDetail`) per prospect with stage, contact, next step, and full activity timeline built from `grid.activities` grouped by lead_id (now loaded into ACTS_BY_LEAD), plus the action buttons. node --check passes (extracted script). STEP 2 (mockup-open tracking that re-ranks the day) and STEP 3 (one-tap "Respondió" + stage controls) still to come. Deploy: `cd portal && wrangler deploy`.
- **2026-05-24 (later x12)** · **Mi día: split "Para contactar" by data-readiness (Mike: a WhatsApp column full of no-number leads is noise).** `ventas-midia.html` render() now puts only leads WITH a WhatsApp/phone in "Para contactar"; leads without a number go to a new "Por conseguir número" lane (compact rows with IG/Sitio links + an "Agregar número" button that PATCHes `whatsapp` via /api/admin/crm/leads/<id> and promotes them to Para contactar on reload). Event delegation now uses closest('[data-id]') so nn-rows work too. Also fixed earlier this turn: the AI panel "Abrir WhatsApp" button was disabled (dead) on no-number leads; it now always acts (opens+logs with a number, copies the message without). Extracted-script node --check passes. Deploy: `cd portal && wrangler deploy`.
- **2026-05-24 (later x11)** · **Mi día cards now show contact info + tap-to-call (Santi couldn't see/call the phone).** Added a `contactRow()` to `portal/src/ventas-midia.html`: each lead card renders "Llamar <número>" (tel:), WhatsApp (wa.me), Sitio, and IG when present; honest "Sin número todavía (agrégalo en CRM completo)" state when not. CSS .contact/.ci added. Extracted-script node --check passes. Deploy: `cd portal && wrangler deploy`. NOTE: most current leads have NULL phone (seed + directory-only vetted clinics), so numbers still need enriching via the Places prospecting engine or manual CRM entry before there's anything to call.
- **2026-05-24 (later x10)** · **Rewrote the AI outreach drafter prompts in `portal/src/outreach.js` (all 4: Santi/Mike, WhatsApp/email).** Fixes from Mike's review of a real draft. New rules baked in: open with a human intro ("Hola, soy Santi de PymeWebPro"), not a cold bot line; state what you OBSERVED ("entré a tu página y no me cargó") and never assert "el sitio está caído/roto" unless verified (avoids false claims from JS-render false positives); close on a FREE MOCKUP offer, not a guilt/quantify-your-failure question; never promise Google #1 / guaranteed clients / results (only real benefits: carga rápida, hecha para convertir); say "página de ventas/reservas," never "sitio web"; lean the gringo angle (captar al cliente extranjero que reserva en inglés) when the business serves tourists/foreigners; consistent "tú", COP only. node --check passes. Deploy: `cd portal && wrangler deploy`.
- **2026-05-24 (later x9)** · **Added an objection-handling section to Santi's guide.** New section 6 "Qué responder cuando dudan" in `portal/src/ventas-guia.html` with 7 agree-then-reframe rebuttals (leading with "Ya nadie usa páginas web" -> we don't make webpages, we make sales pages; plus Instagram-is-enough, price, "let me think", "will it bring clients", family member, "send me info" -> free mockup). Renumbered the following sections (Agregar 7, Recursos 8, Reglas 9, Rutina 10). Points to the full guion in Recursos. Deploy: `cd portal && wrangler deploy`.
- **2026-05-24 (later x8)** · **Built Santi's guided AI sales funnel ("Mi día") + his instruction manual, in Spanish (BUILT, NOT deployed/browser-tested).** `portal/src/ventas-midia.html` is now the default landing on ventas.pymewebpro.com: a 3-column Spanish daily screen (Para contactar / Seguimientos / Por cerrar) on the existing APIs (Access auth, no token). Each card has one "Generar mensaje" that calls `/api/admin/outreach/draft` (first-touch/follow-up/cierre via the `instruct` param), shows the draft to edit, opens WhatsApp prefilled and logs via `/api/admin/outreach/log-send`. Interesado/Ganado/Descartar PATCH `lead_stage`/`status` via `/api/admin/crm/leads/<id>`; Agendar llamada opens the Cal link; "+ Agregar prospecto" POSTs to `/api/admin/crm/leads` (all fields verified against EDITABLE_COLUMNS). Seguimientos reads `/api/admin/outreach/cadence`. Full CRM moved to `/crm`, manual at `/recursos`. `portal/src/ventas-guia.html` (served at `/guia`, "Guía" link in the Mi día header) is Santi's step-by-step manual. index.js imports VENTAS_MIDIA_HTML + VENTAS_GUIA_HTML and routes `/`, `/guia`, `/crm`, `/recursos` on the ventas host. node --check passes on index.js + the extracted Mi día script. **Cal link defaults to cal.com/medellinguide/intro (swap to Santi's later).** CSV intake deferred (manual add + seed + Places engine cover intake). **Deploy:** `cd portal && wrangler deploy`. **Smoke-test:** open ventas, confirm Mi día shows the 15 clinics in Para contactar, Generar mensaje works, Abrir WhatsApp + Interesado/Descartar move cards, + Agregar prospecto creates one, /guia + /recursos + /crm load.
- **2026-05-24 (later x7)** · **Dropped the funnels PDF from the Recursos page (Mike: low value in a mockup + WhatsApp motion, and a static asset that drifts).** Removed the PDF download button from `ventas-recursos.html` (regenerated), the `/recursos/funnels.pdf` route + the `FUNNELS_PDF_B64` import from `index.js`, and deleted `portal/src/funnels-pdf.js` (~52KB). Recursos is now the sales manual only (guion, reglas, calificacion, alcance). Deploy: `cd portal && wrangler deploy`.
- **2026-05-24 (later x6)** · **Recursos page: sticky top bar.** Regenerated `portal/src/ventas-recursos.html` so the header (PDF button) + the doc nav buttons sit in one `.topbar` with `position:sticky;top:0` and stay visible while scrolling; sections got `scroll-margin-top:130px` so anchor clicks land the heading below the bar, not under it. Same generation script as later x5 (python markdown). Deploy: `cd portal && wrangler deploy`.
- **2026-05-24 (later x5)** · **Brought the funnels PDF + sales manual back onto ventas (a Recursos page).** When ventas.pymewebpro.com moved to the worker, the old static docs (still live at pymewebpro-ventas.pages.dev) were no longer at the custom URL. Re-homed the still-useful ones onto the worker, Access-gated under ventas: `portal/src/funnels-pdf.js` (PymeWebPro-Funnels.pdf base64-embedded, served at `/recursos/funnels.pdf`) and `portal/src/ventas-recursos.html` (the manual: guion + reglas + calificacion + alcance, rendered from the pymewebpro-sales markdown via python-markdown). Added a "Recursos" link in the CRM header (crm.js) and `/recursos` + `/recursos/funnels.pdf` routes in the ventas host branch (index.js, imports FUNNELS_PDF_B64 + VENTAS_RECURSOS_HTML). Configurador stays retired. node --check passes on index.js, crm.js, funnels-pdf.js. **To refresh later:** regenerate funnels-pdf.js (`base64 -w0` the PDF) and ventas-recursos.html (python markdown over the 4 pymewebpro-sales docs), then redeploy. **Deploy:** `cd portal && wrangler deploy`.
- **2026-05-24 (later x4)** · **Removed the sales funnel (CRM tab) from the colguides master-portal admin · colguides is now management-only.** Per Mike: the funnel lives only at ventas.pymewebpro.com, no point having it in two places. `portal/src/frontend.html`: removed the CRM nav tab (`tabBtn('crm',...)`), the `/admin/crm` + `/admin/leads` -> 'crm' route mapping (they now fall back to the Clients tab), the CRM `<iframe src="/admin/crm?embed=1">` render block, and the crm-specific full-width `<main>` style. Kept Clients + Mockups (management). The `LeadDetail` component + lead state remain as unreachable dead code (no nav or link reaches them; safe to strip later). `node scripts/check-spa.mjs` passes. The `/admin/crm?embed=1` endpoint still exists but is no longer surfaced anywhere in colguides. **Deploy:** `cd portal && wrangler deploy`.
- **2026-05-24 (later x3)** · **Dropped the admin-token requirement on Access-gated hosts (auth now via Cloudflare Access).** `isAdmin` (portal/src/index.js) ALSO authorizes a request carrying `Cf-Access-Authenticated-User-Email` on a gated host (`ACCESS_ADMIN_HOSTS` = ventas.pymewebpro.com, colguides.com, www.colguides.com); the bearer ADMIN_TOKEN still works as a fallback and is still REQUIRED on the public checkout host portal.pymewebpro.com (not in the set). The ventas host branch injects `window.PWP_ACCESS_OK=true`; crm.js treats that as authed (no token screen) and won't deauth on a stray 401 while Access is present. Net: Mike + Santi just sign in through Access on ventas, no token field. colguides admin SPA unchanged (still works with its token; API now also accepts Access). node --check passes on both files. **Deploy:** `cd portal && wrangler deploy` (+ git push for the repo).
- **2026-05-24 (later x2)** · **Ventas sales funnel v1 + AI-draft button in the CRM + clinic seed (BUILT, NOT deployed).** Stood up ventas.pymewebpro.com as the standalone PymeWebPro sales funnel served by this worker: added the `ventas.pymewebpro.com` route (`wrangler.toml`) and a host branch in `index.js` fetch() that serves the standalone CRM (today/funnel/leads/deals/activities) full-page with its own CSP, reusing `/api/admin/*` + D1 (no new DB, no role-scoping per Mike, both Mike + Santi full access). Wired the existing AI outreach drafter into the CRM lead card: a "Borrador IA" button (`crm.js`) calls `/api/admin/outreach/draft`, shows the Claude-generated first WhatsApp for review, opens wa.me prefilled and logs the send via `/api/admin/outreach/log-send`. Added `seeds/2026-05-24-medellin-clinics.sql` (15 verified clinics; phones NULL pending Places enrichment; `suggested_pitch` = the observed weakness so the drafter personalizes). `node --check` passes on `index.js` + `crm.js`; browser logic NOT runtime-tested (crm.js is a template-literal app, so test in a browser after deploy). Configurador dropped from the funnel (homepage builder + AI drafts replace it). **CUT-OVER, in order:** (1) detach `ventas.pymewebpro.com` from the `pymewebpro-ventas` Pages project; (2) `cd portal && wrangler deploy`; (3) add a Cloudflare Access app for `ventas.pymewebpro.com` (same policy as colguides, Mike + Santi); (4) `wrangler d1 execute pymewebpro-portal --remote --file=seeds/2026-05-24-medellin-clinics.sql`; (5) smoke-test: open ventas, enter admin key, open a lead, click Borrador IA.
- **2026-05-24 (later x1)** · **Fixed the 4 flagged follow-ups from the cleanup sweep.** Hosting año $270.000 → $300.000 everywhere: portal `HOSTING_PRICES_COP.annual`, `checkoutHosting` charge, `getClientProject` option, and customer-facing `hosting.html` (price, meta + og description, "Ahorra 25%" → "Ahorra 2 meses", equivale $22.500 → $25.000/mes, ahorra $90.000 → $60.000). Removed the dead `/privacy` block from `_headers`. Deleted the scraped Instagram dump files under `manual-mockups/revo-cafe/` (Instagram 1.html, Instagram2.html + their _files dirs); real mockup assets untouched. `legal.js` was a FALSE alarm: its privacy.html/sitemap refs are the client-site legal generator (templated per client business), not pymewebpro's deleted page, so no change. `node --check` passes on index.js; grep confirms zero 270.000 left in site+portal. NOT deployed. **Deploy:** site `git push`; portal `cd portal && wrangler deploy`.
- **2026-05-24** · **Colombia/COP cleanup sweep (Mike-directed): removed the NA/Quebec privacy doc, Wise, Ontario governing law, the Crecimiento upgrade SKU, and stray CAD/USD.** Deleted `privacy.html` (English PIPEDA/Quebec policy) and added a forced 301 `/privacy` → `/politica-de-datos` in `_redirects`; removed its footer links from `index.html` + `en/index.html`. `politica-de-datos.html`: dropped the Wise sub-processor, the /privacy hreflang + English link, the GDPR/Quebec mentions (Ley 1581 only now), and a stale "plan Pro" reference. Removed the Wise mention on the homepage deposit step (ES+EN). `contracts/service-agreement-template.md`: governing law Ontario/ADR-Canada → Colombia/Medellín. Removed the $300k Esencial→Crecimiento upgrade SKU from the portal (`portal/src/index.js`: route, `checkoutUpgrade`, `processUpgradePayment`, payment branch, `can_upgrade` option; node --check passes, 0 dangling refs). `crm.js` money formatter CAD→COP. Removed a "(~$490 USD)" note in `memory/outbound-prospecting-plan.md`. NOT deployed. **Deploy:** site `git add -A && git commit && git push` (Pages); portal `cd portal && wrangler deploy`. **Flagged, NOT changed:** portal hosting año still charges $270.000 (source of truth says $300.000) in `checkoutHosting` + `getClientProject` options; `legal.js` sitemap still lists `/en/privacy.html`; `_headers` still has a `/privacy` block; scraped Instagram dumps under `manual-mockups/revo-cafe/` are deploy junk.
- **2026-05-23 (later x1)** · **Completed the flagged single-product + CAD cleanup sweep across production files (the cleanup the earlier 2026-05-23 entry flagged as "NOT yet fixed").** Verified and (where stale) corrected every file from that flag list. Result: ALL of `portal/src/proposal-generator.js`, `manual-mockups/start/index.html`, `contracts/service-agreement-template.md`, `portal/src/santi.js`, `main.js` (price map, including the legit `esencial:annual` $690.000 = base $390k + annual hosting $300k bundle), `portal/src/index.js` (post-pay confirmation copy collapsed to one "delivery clock" line, no tier names), `portal/src/payments.js` + `portal/src/deposit-links.js` (comments now describe the single-product reality + the retained-legacy `pro` key), and `guides/traffic` + `guides/whatsapp-business` (ES+EN) now show the single product, COP-only ("La página de ventas" $390.000 COP + à la carte add-ons), no Essential/Pro/Crecimiento tier language, no CAD/USD figures, no em dashes. Two files needed actual edits this pass: (1) `portal/src/chief-of-staff.js` line 80 example string ("el precio Pro sube a $750k COP" → "el precio de la página de ventas sube a $450.000 COP"); (2) `portal/src/chief-of-staff-knowledge.js` regenerated via `node portal/scripts/build-chief-knowledge.mjs` (it is auto-generated, "NO editar a mano") so it now embeds the corrected `glossary.md`/`studio.md` (which already carried the single-product facts + NA/CAD retirement notes). `node --check` passes on both edited JS files. Remaining `CAD`/`USD` strings in the swept files are all legitimate (brand rules telling agents never to quote CAD/USD, "no CAD/USD" code comments, and historical retirement notes in memory docs); remaining 2 em dashes in the regenerated knowledge bundle come from embedded CLAUDE.md changelog history (lines 387-388), left as-is (hand-editing the generated file is forbidden and rewriting historical changelog prose is out of scope). **NOT deployed.** **Deploy:** site (guides/manual-mockups/static) = `git add -A && git commit && git push` (Pages); portal worker (proposal-generator, santi, chief-of-staff, chief-of-staff-knowledge, index, payments, deposit-links) = `cd ~/code/pymewebpro/portal && wrangler deploy`. **Flagged for human review (OUT of this sweep's assigned scope, not edited):** `privacy.html` + `politica-de-datos.html` still list **Wise Payments (CAD/USD) for North America / Canada clients** (contradicts Colombia-only/COP-only/Wompi-only · these are customer-facing legal pages); `portal/src/crm.js` money formatter defaults non-COP to "CAD $" (internal admin only); `memory/outbound-prospecting-plan.md` has a "(~$490 USD)" parenthetical on a COP figure; `portal/src/index.js` `checkoutUpgrade`/`processUpgradePayment` (the Esencial→Crecimiento $300k upgrade SKU) is still wired and its customer email still describes the Crecimiento upgrade · left intact because it is a live, reachable flow with stored-data implications (`plan='pro'`), not stale launch pricing; `contracts/service-agreement-template.md` governing law is still "Province of Ontario, Canada" (a legal-jurisdiction clause, not a price/currency item).
- **2026-05-23** · **Fixed stale single-product drift in this CLAUDE.md (Pricing/Hosting/Payment + 2 glossary rows still said Essential/Pro $390k/$690k; production pivoted to single-product on 2026-05-20).** Rewrote those sections to the one product (la página de ventas $390.000 + à la carte add-ons), aligned to source of truth `~/code/pymewebpro-sales/internal/add-on-menu.md` (also corrected hosting año $270k→$300k). Docs only, no code touched. Also fixed README.md in the sales folder; confirmed `terminos.html` already single-product. **Flagged, NOT yet fixed (needs an approved cleanup pass):** broader single-product + CAD drift in production code · `portal/src/proposal-generator.js` (Pro $690k + CAD prices), `manual-mockups/start/index.html` (CAD + Essential/Pro), `contracts/service-agreement-template.md` (CAD + tiers), `portal/src/santi.js` (Pro $690k card), `portal/src/chief-of-staff.js` + `chief-of-staff-knowledge.js` (agent knowledge), `main.js` (price map with CAD-comma format), `guides/traffic` + `guides/whatsapp-business` (Essential/$690k copy). CAD references are hard-rule violations.
- **2026-05-21 (later x2)** · **Cache-busted the OG image URL (the re-render alone wasn't enough · stale edge cache).** After (later x1) deployed the corrected JPG, the social card was STILL showing the garbled image: `screenshots/og-pymewebpro.jpg` is served with `cache-control: public, max-age=2592000, immutable`, so the Cloudflare edge (and the social platforms) stayed pinned to the old broken file at that exact URL for ~30 days even though origin had the new one (verified: bare URL = 36495B stale HIT, `?cb=` = 77203B fresh from origin). Fix: appended `?v=20260521` to every `og:image` + `twitter:image` so caches treat it as a new resource. Touched `index.html`, `en/index.html`, `tienda/index.html`, `sitios-web/index.html` (6 meta tags). Confirmed the versioned URL returns the new 77203B image from origin. **Deploy:** `git add -A && git commit && git push` (Pages). **After deploy:** re-scrape in Facebook Sharing Debugger + LinkedIn Post Inspector. **Future note:** because the JPG is `immutable`-cached, regenerating it in place won't propagate · always bump the `?v=` (or purge the CF cache for that URL) when the OG art changes.
- **2026-05-21 (later x1)** · **Fixed the broken social-share (OG) image for pymewebpro.com.** The shared image `screenshots/og-pymewebpro.jpg` was garbled (the big headline had collapsed into overlapping glyphs and the wordmark spacing was broken) because it had been re-rasterized without the Fraunces / Inter Tight web fonts. Re-rendered from the existing source `screenshots/og-pymewebpro.svg` (design unchanged: "Convierte visitantes en clientes.", 1200x630, cream/ink/orange): downloaded the exact Fraunces 500/600 + Inter Tight 500/600/700 TTFs from Google Fonts and installed them locally, stripped the unreachable `@import` line, rendered with cairosvg → PNG, flattened to JPG (quality 90, ~77KB) with ImageMagick. The `og:image` / `twitter:image` meta in `index.html` already point here, so no markup change. **Deploy:** `git add -A && git commit && git push` (Pages auto-deploys). **After deploy:** re-scrape the URL in Facebook Sharing Debugger + LinkedIn Post Inspector to bust their cached OG image.
- **2026-05-21** · **Added Facebook domain-verification meta tag to pymewebpro.com.** For Meta Business domain verification: `<meta name="facebook-domain-verification" content="m8somhyzg26z2fspg042uhsiwnax51" />` inserted in `index.html` `<head>` after `color-scheme`. Root only (`en/index.html` untouched · Meta verifies the domain root). No CSP impact (meta tags aren't script/style). **Deploy:** `git add -A && git commit && git push`, then click Verify in Meta Business → Domains.
- **2026-05-20 (later x19)** · **Checkout was DOWN · restored portal.pymewebpro.com (public host) + fixed post-pay redirect.** Mike's /start "Continuar al pago" did nothing / "Error de red". Root cause (pre-existing since 2026-05-13): `portal.pymewebpro.com` was retired from `portal/wrangler.toml` routes, but the public checkout still depends on it · `/start` POSTs to `https://portal.pymewebpro.com/api/leads` and redirects to `/c/:id` there (and `mockups.js` APP_URL fallbacks). With no route, the fetch failed. **Fix:** (1) re-added `{ pattern = "portal.pymewebpro.com", zone_name="pymewebpro.com", custom_domain = true }` to `wrangler.toml` routes (admin stays gated at colguides; the worker's own login still protects admin on this host). (2) `handleCreateCheckout` redirect-url now uses the request origin (the public checkout host) instead of `env.APP_URL` (the Access-gated colguides mount), so the post-payment return page is reachable. `node --check` passes. **DEPLOY:** `cd ~/code/pymewebpro/portal && wrangler deploy` (wrangler auto-creates the portal.pymewebpro.com custom domain + DNS; if it complains about an existing `portal` DNS record, delete that record in the CF dashboard and redeploy). Then test /start end-to-end. **Note:** admin is reachable again at portal.pymewebpro.com (behind the worker login, as pre-2026-05-13).
- **2026-05-20 (later x18)** · **MONEY-CRITICAL · checkout now charges 30% of the CONFIGURED total (calculator → /start → Wompi all reconcile).** Mike: "if someone picks a million in add-ons they should be charged a million" (30% start / 70% launch, full total via Wompi). **Discovery:** `portal/src/index.js` `computeQuote` was charging the FULL base plan price ($390.000) at `/c/:lead` checkout, ignoring both the 30% deposit AND the add-ons. **Fix (portal):** `computeQuote` now reads the configured total from `lead.metadata.extra.total_cop` (persisted by `captureLead`), sets `projectTotal` = that (fallback base+hosting), and charges a **30% deposit** (`total_cents = round(projectTotal*0.30)*100`); exposes `project_total_cop`/`deposit_cop`/`balance_cop`. `confirmationHtml` now shows Total del proyecto / Saldo al lanzar (70%) / Depósito hoy (30%). **Fix (front-end):** calculator (index + en) and `/start` deposit reverted from the fixed $117k to `round(total*0.30)`; labels back to "30%/70%". Verified: $770k config → deposit $231.000 at all three layers; $1M → $300.000. `node --check` on portal passes; check:standards 0 fails. **DEPLOY (two separate):** site `git push` (Pages); **portal `cd ~/code/pymewebpro/portal && wrangler deploy`**. **⚠ Mike MUST test a real Wompi transaction** before relying on it (sandbox can't test live payments). **Note:** base page now charges $117k (30%) at /start, not $390k full (this is what the site always promised). The 70% balance is still collected at launch (existing flow). Valentina's own deposit flow (`pymewebpro-agent/src/payments.js`) still charges base-page 30% only and doesn't see calculator add-ons (separate surface; flag if you want it wired too).
- **2026-05-20 (later x17)** · **Calculator ↔ /start now connected; removed extra-page add-on (ES+EN).** Mike: the calculator and `/start/?plan=esencial` were disconnected (selections lost). The section-5 builder's "Empezar/Get started" button (`#bs-start`) now builds its href dynamically in `calc()`: `/start/?plan=esencial&total=<n>&monthly=<n>&items=<encoded list of selected add-ons>`. `start/index.html` reads those params: shows the configured total as the price, "Depósito hoy ($117.000) reserva tu cupo · Saldo al lanzar $<total−117000> · incluye: <items>", and rides `total_cop`/`items`/`monthly_cop` along in the lead `extra` + message so the rep sees exactly what to build/invoice. Deposit stays $117.000 (the Wompi charge via portal key `esencial`), which now equals "Depósito hoy" — all three numbers reconcile end-to-end (verified: CRM+chatbot → total 770k, dep 117k, balance 653k). Also removed the "Página adicional (máx. 3)" qty add-on from the builder per Mike (hosting-by-month stepper still works via the generic `.addon-qty` handler). CSP refreshed (68/17), all inline scripts (index, en, start) parse, check:standards 0 fails. **Deploy:** `git add -A && git commit && git push`. **Still open:** Section 6 form→pay rebuild; EN FAQ parity; heading order; brand-orange contrast decision; `REPLACE_` placeholders.
- **2026-05-20 (later x16)** · **Bug hunt (2 QA subagents) + CSP-breakage fix + calculator/checkout match (ES+EN).** Mike reported "we've got problems"; spawned two agents to audit. Root cause: the marketing-site CSP (`functions/_middleware.js` `CSP`, served to all non-mockup pages) is **hash-locked with NO `'unsafe-inline'`/`'unsafe-hashes'` in `style-src`, so it strips every inline `style="..."` attribute** at runtime. Several inline styles were present (pre-existing footer brand colors = the recurring "< web />" bug, the spam honeypot `left:-9999px` becoming a visible field, FAQ link colors, the footer blurb/legal-center) plus my new "Ver prueba" proof links. **Fix:** moved ALL inline `style=""` into the hashed `<style>` block as classes (`.u-offscreen`, `.u-orange`, `.u-tc`, `.receipt-proof`, `.foot-blurb`); both files now have **0 inline style attributes**. EN footer brand de-inlined (CSS `footer.site .brand` already styles it). Also fixed: EN `href="#hook"` → `#top` (nav, footer brand, footer chapter link were dead; hero id is `top`); EN refund copy "dollar" → "peso" (COP-only rule); **calculator/checkout mismatch** (Mike): the builder computed deposit as 30% of the configured total but `/start/?plan=esencial` only charges 30% of the base, so the deposit is now LOCKED at **$117.000** (reserves the build slot, matches Wompi), with add-ons in the "Saldo al lanzar". Labels updated ("Para arrancar · depósito" / "Saldo al lanzar"). CSP refreshed (68/17), inline scripts parse, check:standards 0 fails. **STILL OPEN:** (a) **Section 6 still a lead form** — Mike wants it to be the PAY step (remove the form, lead with "pay the $117.000 deposit"); NOT done yet. (b) EN FAQ missing the "full website / online store" question (ES has it). (c) heading order (h1→h4→h5 skips). (d) brand orange `#FF5C2E` text contrast (2.91, brand decision). (e) BP console-error exact text + Trusted-Types ceiling. (f) placeholders `REPLACE_WITH_NAMECHEAP_REFERRAL_URL` + `REPLACE_WITH_SANTIAGO_HANDLE`. **Deploy:** `git add -A && git commit && git push`.
- **2026-05-20 (later x15)** · **A11y/BP fixes from the PageSpeed audit (ES+EN) · the safe, non-brand ones.** Audit showed a11y 91 (contrast, links-rely-on-color, heading order), BP 92 (console errors, CSP-XSS/Trusted-Types). Computed real WCAG ratios. Fixed: (1) **lang-switch** removed `opacity:.7` on the tan link (was 3.52 → 7.12). (2) **Footer fine-print** `#7A6957` → `#A89788` (legal block, copyright, logo brackets; 3.45 → 6.45). (3) **Links-rely-on-color:** underline added to in-prose links (`.faq .answer a`, `footer .copy a`, `.hero-traffic a`). (4) **og:image 404:** meta pointed to `og-pymewebpro.jpg` which didn't exist (only `.svg`); rasterized a real 1200×630 `screenshots/og-pymewebpro.jpg` via ImageMagick (kills the 404 + fixes social previews). CSP refreshed, check:standards 0 fails. **NOT done · need Mike's call / more info:** (a) **brand orange `#FF5C2E` fails contrast** (2.91 on cream, below even the 3:1 large-text bar) on the hero H1 accent, inline links, and card arrows · fixing means darkening to ~`#C73E15` which changes the look · DECISION PENDING. (b) **Heading order** (h1→h4→h5 skips) · a clean tag+CSS restructure, not yet done. (c) **Console errors** · the og 404 was likely one; need the exact remaining error text from the BP audit. (d) **Trusted Types / strict CSP** · large refactor (breaks innerHTML in builder/carousel), realistically caps BP shy of 100. **Deploy:** `git add -A && git commit && git push`.
- **2026-05-20 (later x14)** · **Best Practices fixes (ES+EN) · perf hit 97, now polishing a11y/BP.** After deploy, mobile Performance reached 97 (a11y 91, BP 92, SEO 100). (1) Added explicit `width`/`height` to all 20 images that lacked them (6 build shots 800×600/4:3, 2 founder avatars 96×96/1:1) → fixes the BP "image elements do not have explicit width and height" + reserves layout space. The pexels gallery imgs keep their 1600×900 attrs (16:9, matches the now-800×450 file ratio, so no CLS). (2) Removed the 2 inline `onerror="this.parentNode.textContent=..."` handlers on the founder photos (CSP-unfriendly; photos exist so the fallback was moot). Body-HTML only, CSP still in sync (68/17); `check:standards` 0 fails; no em dashes. **Still open:** Accessibility 91 + the rest of Best Practices 92 need the specific PageSpeed audit items (likely color-contrast for a11y) before targeting precisely. **Deploy:** `git add -A && git commit && git push`.
- **2026-05-20 (later x13)** · **Performance fixes (Mike: "fix the problems, don't soften the claims") · ES+EN.** PageSpeed mobile was 87 perf with a 10.2MB page, ~1,850ms render-blocking, and a 726KiB image-delivery flag. Root cause of the weight: `screenshots/patrick-circle.webm` was **7.5MB** and autoplays on load. **(1) Video:** first recompressed the autoplay testimonial loop, then per Mike REMOVED it entirely (he found it boring + it was an English-audio talking head on a Spanish-primary site). Deleted the `.quote.has-video` avatar/video, the `<dialog>` modal, and the modal IIFE (ES+EN); kept Patrick Detzner's written (translated) review as a normal text quote. Net: zero testimonial-video bytes on load. The now-unused `screenshots/patrick-circle.{webm,mp4}` + `patrick-full.mp4` are unreferenced dead files (sandbox can't unlink · Mike can `rm` them). Dead `.vid-*` CSS selectors left in place (harmless). **(2) Fonts:** Google Fonts stylesheet was render-blocking; now loads non-blocking via `media="print"` + a CSP-safe hashed inline flip script (`link.async-font` → media='all'; no inline onload, since CSP is hash-locked) + `<noscript>` fallback. Kept `display=swap`. **(3) Stale preload:** removed `<link rel=preload as=image ... schedulator.jpg fetchpriority=high>` (the old hero image; the new Section-0 hero is text, so it was a wasted high-priority fetch competing with LCP). **(4) Pexels gallery:** the 12 carousel images dropped from `w=1600&h=900` to `w=800&h=450` (same 16:9, so width/height attrs unchanged → no CLS); they were already `loading=lazy`/`data-src` except the active one. Cache: `/screenshots/*` already had 30-day immutable (the 4KiB "cache lifetimes" item was negligible). CSP refreshed (68 scripts incl. 2 new font-flip, 17 styles); `check:standards` 0 fails; hero render-verified (fonts still apply); no em dashes. **Still open (need the actual audit items):** Accessibility 91 + Best Practices 92 · expand those sections in PageSpeed (or re-run after deploy) so they can be targeted; the remaining perf items (forced reflow, 1 non-composited animation, 1 long task) are minor. **Deploy:** `git add -A && git commit && git push`.
- **2026-05-20 (later x12)** · **Homepage polish pass (ES+EN, Mike rapid iteration).** (1) **Founder card:** Mike's tagline → "Cofundador canadiense · gestiona las construcciones" / "Canadian co-founder · runs the builds" to match Santi's format; removed his Cal.com call-booking pill. (2) **No call-booking for Mike anywhere:** removed every `cal.com/mike-chartrand/15min` link, founder card + contact "talk first" block + start wizard + the footer "Hable con Mike"/"Talk to Mike" item across ALL ~28 pages (homepages, sitios-web, tienda, guides) via sed. Left WhatsApp Santiago as the human-contact path. (NOT touched: `manual-mockups/start/index.html` still has 2 demo "Book a call" CTAs · it's a mockup template behind rebuild-mockups, flagged.) (3) **Live builds grid:** dropped Marena, moved Inviersol to the front (real clients first: Inviersol, Schedulator, Blue Whale; then concept mockups). (4) **Proof links:** added "Ver prueba / See proof →" under the 100 / <0.9s / A+ stats, linking live PageSpeed Insights (speed/Lighthouse) + Mozilla Observatory (A+) reports for pymewebpro.com. (5) **FAQ reorder:** free-guides Q now sits under "¿Garantizan leads?/Do you guarantee leads?" (schema didn't include the guides Q, so no JSON-LD change). (6) **Primary CTA relabeled** from "Hablar con Valentina/Talk to Valentina" → "Obtén respuestas/Get answers" (nav, hero, builder), still `data-valentina-open`; kept the "Responde en segundos · sin formularios" subline. (7) Hero CTA given a 32px top gap so it separates from the 6-part grid. (8) **Section 5 price builder** gained hosting options: buy by the month ($30.000/mes, qty stepper) or by the year ($300.000, "ahorra dos meses" / "saves two months"); builder JS refactored to a generic `.addon-qty` (data-qprice/data-qmax) handler so página-adicional + hosting-meses both work. CSP refreshed (67 scripts/17 styles, in sync), `check:standards` 0 fails, zero em-dash/CAD/USD. **Still OPEN (Mike to weigh in):** Section 6 push-to-pay reframe; real Namecheap referral URL (domain FAQ placeholder); which section links to the guides/each type; reword the guarantee from "if the page doesn't work" to "if you're not happy". **Deploy:** `git add -A && git commit && git push`.
- **2026-05-20 (later x11)** · **`terminos.html` rewritten to the single-product model (Mike approved the "2 rounds + 30-day money-back" basis).** Legal Terms of Service was still the stale two-plan version (Plan Essential $390k / Plan Pro $690k) with contradictory, outdated commitments (multi-página, hosting 1 año/2 años, "1-2 semanas de revisiones", and a **$75/hr CAD** edit rate that broke the COP-only rule). Section 2 now describes ONE product (la página de ventas $390.000 COP pago único + the à la carte add-on menu with prices); tienda real / blog / sitio multi-página / apps / membresías / portales explicitly "fuera de la página de ventas, se cotizan por separado". Section 4 (tiempos) single product, 48h. Section 5 (revisiones) → "2 rondas incluidas; rondas adicionales $90.000 COP c/u", CAD rate removed, "no revisiones ilimitadas". Updated the visible "última actualización" date + the WebPage JSON-LD `lastReviewed` to 2026-05-20. Ran `refresh-csp-hashes.py` (the JSON-LD hash changed); `check:standards` 0 fails; zero CAD/USD/em-dash/Essential-Pro left (only the "PymeWebPro" brand). **Deploy:** ships with the homepage push (`git add -A && git commit && git push`).
- **2026-05-20 (later x10)** · **Homepage Section 0 hero + interactive public price builder + traffic section moved (ES+EN).** Three changes, both `index.html` and `en/index.html`, driven by Mike's objective "sell the idea of a marketing/landing page, most SMB owners don't know what it is." (1) **Section 0 hero:** replaced the old 2-column hero (chapter-01 marker + "No necesita un sitio web" + ingredients card + lead-form CTA) with a single-column Section 0 that frames the idea: eyebrow "Páginas de ventas de alta conversión · Colombia" / "High-conversion marketing pages · Colombia", H1 "No un sitio web. Una *página de ventas* de alta conversión." / "Not a website. A high-conversion *marketing page*.", lede "Un sitio web informa. Una página de ventas hace un solo trabajo: convertir visitantes en clientes.", the 6-part grid intro "Toda página de alta conversión tiene 6 partes. Esta es una. Bájela y véalas en acción:" (cards re-captioned), CTA "Hablar con Valentina ahora / Talk to Valentina now" (data-valentina-open) + "Responde en segundos · sin formularios", and a traffic teaser link at the bottom. Removed the chapter-01 marker + the micro trust line. CSS reworked: `.hero .wrap` grid→single column, ingredients-block goes transparent (no white card), new `.hero-rule`/`.hero-traffic`/`.cta-note`. (2) **Public price builder** replaces the static two-card offer in Chapter 05: base "La página de ventas $390.000" (fixed, with includes) + add-on checkboxes (GBP 90k, advanced analytics 120k, CRM 190k, bilingual 75k, copywriting 150k, catalog 180k, PDF 50k, extra page 50k qty max 3, chatbot 190k+80k/mes) → live total + 30/70 split + chatbot-only monthly line, CTAs "Empezar" (/start/?plan=esencial, keeps portal key) + "Hablar con Valentina". New inline `<script>` per file + `.builder*`/`.bs-*` CSS. No rep-only scope flags. (3) **Traffic/guías library section moved** from between ch05 and ch06 to just above the footer (after the FAQ); the `11 guías` link was also dropped from the top announcement bar and the nav CTA changed to "Hablar con Valentina"/"Talk to Valentina" (data-valentina-open). Ran `python3 scripts/refresh-csp-hashes.py` (67 scripts incl. the 2 new builder scripts, 17 styles). `cd portal && npm run check:standards` → 0 fails. Both hero + builder render-verified with WeasyPrint. Zero em dashes / CAD / USD / Essential-Pro. **NOT deployed.** **Deploy:** `git add -A && git commit -m "Homepage: Section 0 hero + interactive price builder + traffic moved down" && git push`.
- **2026-05-20 (later x9)** · **Single-product pivot · homepage + start wizard collapse Essential/Pro into ONE product + à la carte add-ons (Mike: "why dont we offer one marketing page with addons").** `index.html` (ES) + `en/index.html` (EN) offer section (Capítulo 05): the two `.tier` cards (Essential $390.000 / Pro $690.000) are now ONE featured product card ("La página de ventas" / "The marketing page" · $390.000 · base includes the full feature list incl. 6-step structure + 1 month hosting + 2 rounds) beside an **Add-ons panel** (GBP $90k, advanced analytics $120k, CRM $190k, bilingual $75k, copywriting $150k, simple catalog $180k via portal, PDF button $50k, extra page $50k, chatbot $190k+$80k/mes), with a "chatbot is the only monthly" note. JSON-LD: two `Service` blocks → one ("Página de ventas"/"Marketing page", price 390000 COP). Hosting FAQ (visible + schema FAQPage) "Esencial 1 mes; Pro 1 año" → "incluye 1 mes". `start/index.html`: dropped the Essential/Pro tier wording (PLANS now a single entry, kicker/message reworded, `extra.hosting` always `monthly`) BUT kept the portal key `plan="esencial"` so the Wompi checkout/pricing contract (`/api/leads` → `/c/:lead_id`) is unchanged. The unlinked `?plan=pro` ($690) self-serve path is now retired (falls back to the base page). `terminos.html` still lists the old two plans (stale, contradictory legal commitments) and needs a Mike-confirmed rewrite (NOT done, flagged). Ran `python3 scripts/refresh-csp-hashes.py` (67→65 scripts, the 2 removed JSON-LD blocks); `cd portal && npm run check:standards` → 0 fails; zero em dashes, zero CAD/USD, zero Essential/Pro/$690 left on either homepage or the start wizard. **NOT deployed.** **Deploy:** `git add -A && git commit -m "Single-product: one marketing page + add-ons (homepage + start wizard)" && git push`.
- **2026-05-20 (later x8)** · **Chatbot reframed from Pro feature to optional add-on (ES + EN).** Per Mike, the chatbot is now an add-on on either plan, not included in Pro. Removed "Chatbot de captura de leads" / "Lead-capture chatbot" from the Pro card includes; replaced the Pro-card chatbot note (which said renewal becomes $110.000/mes) with an add-on note ("optional add-on on any plan: $80.000 COP/mo; the funnel is one-time, only the chatbot adds a monthly"). No `$110.000` left on the site. CSP unchanged (only `<li>`/`<p>` content, no inline script/style blocks), `refresh-csp-hashes.py` reports in sync; `check:standards` green on both files. Part of a cross-surface pass (also guide docs + Valentina). Deploy: `git push`.
- **2026-05-20 (later x7)** · **Trimmed 4 FAQ questions + reworded the FAQ heading (ES + EN).** Removed "is this just a one-page website", "who actually builds it", "can you write the copy or do I need to", and "what happens after launch" from both the visible `<details>` accordion and the FAQPage JSON-LD (kept visible + schema in sync, so Google sees no orphan FAQ entries). Two of the four were in the schema (one-page, who-builds); the other two were visible-only. Heading "Antes de decir que sí." / "Before you say yes." → "Preguntas frecuentes." / "Frequently asked questions." (default; easy to reword). Ran `refresh-csp-hashes.py` (JSON-LD is inline). `check:standards` green on both files. Deploy: `git push`.
- **2026-05-20 (later x6)** · **Footer "< web />" logo bug fixed for good + automated guard so it cannot regress again.** Root cause of the recurring regression (3rd time): the brand CSS is inline-duplicated in each language file, and the `footer.site .brand{color:#FBF7F0}` light override existed only in `index.html` (ES), never in `en/index.html` (EN). On EN the brand inherited the default dark `.brand` color (#1B1410) on the dark footer, so "pyme"/"pro" went invisible and only orange "web" survived. Fix: added the three `footer.site .brand` rules to `en/index.html`. **Prevention:** added a FAIL-severity check to `portal/scripts/check-standards.mjs` ("Footer brand legible on dark footer") that asserts BOTH production pages contain the light `footer.site .brand` override; it ran red on EN before the fix (proven to catch it) and green after. It runs on every deploy via the `predeploy: npm run check` hook, so a missing override now blocks the build. Ran `refresh-csp-hashes.py` (EN style block changed). Deploy: `git push`. Durable follow-up (bigger): the underlying cause is per-language inline-CSS duplication with no sync; extracting a shared stylesheet (or a check that diffs the ES/EN `<style>` blocks) would prevent the whole class of ES/EN drift, not just this rule.
- **2026-05-20 (later x5)** · **Fixed the Guides breadcrumb 404 on the traffic pillar.** The pillar pages (`guides/traffic/index.html` + `en/guides/traffic/index.html`) had their "Guías"/"Guides" breadcrumb (visible nav + JSON-LD BreadcrumbList) pointing at bare `/guides/` (and `/en/guides/`), which has no index page → 404. Repointed both to `/guides/traffic/` (the pillar hub, which is what all 10 sub-guides already use for that crumb). Ran `refresh-csp-hashes.py` (JSON-LD is an inline script, so its hash changed). No bare `/guides/` links remain anywhere. Deploy: `git push`. Follow-up option: build a real `/guides/` index hub if we want a dedicated listing page.
- **2026-05-20 (later x4)** · **Restored the Valentina chat widget on the marketing site (it had been dropped).** Re-added `<script src="https://valentina.pymewebpro.com/widget.js" defer></script>` before `</body>` in `index.html` + `en/index.html`. The embed was removed back in commit `9825262` ("flatten build:bilingual indirection") and the loss only became visible on the live site after the later x3 push triggered a fresh deploy. CSP already allowed `valentina.pymewebpro.com` in script-src/style-src/connect-src, and external `<script src>` is not hash-locked, so NO CSP refresh was needed. `check:standards` still ✓ on both files. The `data-valentina-open` CTA triggers from the older Valentina-first layout were NOT restored (the page currently uses the lead-form layout); the floating chat launcher works on its own. Deploy: `git push`.
- **2026-05-20 (later x3)** · **Marketing site aligned to the sales guide · CAD nuked, COP-only, product facts synced.** Reconciled `index.html` (ES) + `en/index.html` (EN) to the new sales system (source of truth: `~/code/pymewebpro-sales/`). **Currency:** removed every CAD figure, deleted the `geo-co`/`geo-na` geo-toggle (CSS rules + the `/cdn-cgi/trace` IIFE block in both files) and all `price-na` spans; the site is now COP-only everywhere (hero, topbar, plan cards, deposits, FAQ, footer, schema priceCurrency 390000/690000 COP, areaServed ["CO"]). **Positioning (per Mike):** Canadian-owned kept as a SUBTLE trust signal, not the headline, no superiority tone. "Liderazgo canadiense / North American account management" why-us block reframed to "Un estudio real, con entidad en Colombia · propiedad canadiense + socio paisa, los dos en Medellín". Footer + founder bio note Mike is "canadiense en Medellín hace 15 años". London, Ontario kept as "Estudio canadiense / Canadian studio" beside the Medellín studio (restored in footer + schema address). **Product facts:** chatbot added to Pro card + $80k/mo run-fee + $110k/mo renewal note; booking/citas moved to BOTH plans (was Pro-only); GBP moved Essential→Pro; Maps stays Essential; testimonials + privacy-first analytics confirmed standard; renewal $270.000/año (25%) → $300.000/año (ahorra dos meses) in body + schema FAQ; "no un chatbot" contact promise softened to "una persona real, no una respuesta automática". **CSP:** ran `python3 scripts/refresh-csp-hashes.py` (67 scripts / 17 styles) since inline script/style content changed. **Verify:** `cd portal && npm run check:standards` → both root files ✓, 0 fails; zero em dashes; zero CAD/USD. **NOT deployed.** **Deploy:** `git add -A && git commit -m "Align marketing site to sales guide: COP-only, product facts" && git push`. Also updated the Canadian-positioning rule in this file + `~/code/CLAUDE.md` (Canadian-owned now allowed as subtle trust signal).
- **2026-05-20 (later x2)** · **Landing-page-fit scoring lens + server-side bulk prospecting (~5,000 broad-SMB pull).** New broader thesis: even businesses WITH good sites need conversion landing pages, so a digitally-mature business is a PRIME landing-page customer. Added a SECOND independent score alongside the existing `computeFitScore` (which rewards no-site, unchanged). **Migration** `migrations/0009_landing_score.sql` (NOT 0008 · `0008_mockup_prospects.sql` already existed; renumbered to avoid clobbering it): adds `leads.landing_score REAL` + `leads.landing_heat TEXT` (idx on landing_heat) and a new `prospecting_jobs` table (id/status/target/inserted/skipped/cursor/cities/verticals/notes/timestamps, idx on status) so a cron can chew a large pull and resume from a JSON cursor. **enrich.js:** new `export function computeLandingScore(lead)` (pure, +25 reachable site, +15 social, +15 reviews>=50, +10 rating>=4, +15 phone/WA, +10 address, +10 followers>=1000, cap 100, same heat tiers); wired into `enrichLead` 'full' mode so it writes landing_heat/landing_score in the same UPDATE. **prospecting.js:** expanded SEED map to 25 broad-SMB verticals (kept original 4 ICPs) + 5 metros (Medellín/Bogotá/Barranquilla/Cali/Cartagena); the load path now writes BOTH scores on every insert; added `export async function runBulkProspectBatch(env,{maxPlacesCalls})` (loads oldest running job, resumes cursor, paginates Places via nextPageToken, dedups by place_id then name+city, inserts source='outbound' with dual scores + metadata{bulk_job_id,...}, one summary `activities` row per run, caps 60 Places + 400 inserts/run, separate KV cap `prospecting:bulk:<date>` at 2000/day) plus HTTP routes `POST /api/admin/prospecting/bulk-start` and `GET /api/admin/prospecting/bulk-status` (covered by the existing `/api/admin/prospecting` catch-all mount, no new mount needed). **index.js scheduled():** imports `runBulkProspectBatch`, branches on `event.cron` (14:00 = cadence sweep, every run including 14:00 = bulk batch), both in try/catch + ctx.waitUntil. **wrangler.toml:** `crons = ["0 14 * * *", "0 */2 * * *"]`. Uses existing `env.GOOGLE_PLACES_API_KEY` (no new secret). `node --check` clean on all 3 JS files; zero em dashes; no USD/CAD. **NOT deployed, migration NOT applied.** **Apply + deploy:** `cd portal && wrangler d1 execute pymewebpro-portal --remote --file=migrations/0009_landing_score.sql && wrangler deploy`. **Arm a 5000 job (D1):** see report / use `POST /api/admin/prospecting/bulk-start {"target":5000}` (defaults to all 25 verticals x 5 cities). Then the 2-hourly cron drains it.
- **2026-05-20 (later)** · **Fixed footer brand mark invisible ("< web />" bug).** `footer.site .brand` was inheriting `color:#1B1410` from the `.brand` CSS rule (dark on dark footer background). Inline `style="color:#FBF7F0"` on the `<a>` and `.word` spans was present in the HTML but Chrome's CSSOM was not applying it via `setAttribute`. Fix: added explicit CSS rules `footer.site .brand{color:#FBF7F0}`, `footer.site .brand .br{color:#7A6957}`, and `footer.site .brand:hover .br{color:#FF5C2E}` to override the default `.brand` dark color in the footer context. Removed the now-redundant inline style attributes from the footer brand HTML. Files: `index.html`. Deploy: `git add -A && git commit -m "Fix footer brand visibility" && git push`.

- **2026-05-20** · **Second prospect mockup built via the playbook · `revo-cafe` (Café Rev).** Café de especialidad + brunch en Cra 73 #4-6, Medellín · @caferevmedellin. First mockup built via the new "Copy build prompt" button workflow from the studio admin. Brand-aware bespoke build: mint-teal capybara palette + terracotta accent (their food-photo warmth), Playfair Display italic + Work Sans (distinct from CF's Fraunces+Inter), Spanish paisa-friendly voice, no template inheritance. Hero rotates 3 cropped JPGs from their IG (salmon bagel · bagel platter · interior) on a 5/6 aspect padding-top frame. Sections: hero with "Café en tu barrio" tagline · brunch (3-card menu, Smoked Salmon Bagel + Chili Cheese Omelette + Mini bagel platter) · café especialidad with cold brew tonic + "Blindfolded latte test" sticker · domicilios y para llevar (3-step + coverage pills in Estadio/Laureles/Belén La Mota) · el espacio · **The Capy Project Foundation** dedicated callout linking to @thecapyproject in Orocué, Casanare (real conservation partner, not decoration) · visítanos · footer + chat fab routed to the asistente at `/api/chat/revo-cafe`. Image processing: 6 IG screenshots cropped with `convert` (cutting IG UI) and quality-82 JPGs (~600KB total). **Placeholders for Mike to confirm via iteration notes:** owner first name (used "Alejo"), owner WhatsApp digits (used `573001234567`), exact hours (used Lun-Dom 8-8), delivery coverage barrios. **Files added:** `manual-mockups/revo-cafe/{index.html, hero-*.jpg, menu-*.jpg, mascot-sticker.jpg}` (6 JPGs + HTML), `portal/src/manual-mockups-revocafe.js` (auto-generated). **Files changed:** `portal/src/manual-mockups.js` (registered slug), `portal/src/chief-of-staff.js` (toolListMockups). Bundle: 45KB raw / 1077KB bundled. **Deploy:** `cd portal && npm run check && wrangler deploy`. **Next:** Mike confirms owner name/WA/hours, flips status to `live`, writes the chatbot_system_prompt with the Café Rev menu + Capy Project context.
- **2026-05-19 (later x13)** · **Path-aware CSP for /manual-mockups/* · fixes unstyled mockup pages.** Followup to x12. After moving mockups to pymewebpro.com Pages, every mockup rendered unstyled because the `functions/_middleware.js` Pages function applies the marketing site's **hash-locked CSP** to every HTML response. The hash collector (`refresh-csp-hashes.py`) explicitly excludes `manual-mockups/` from its scan, so the mockup's inline `<style>` blocks have hashes NOT in `STYLE_HASHES`, and the browser silently blocks them. Symptom: mockup pages load but appear without any CSS. Fix: added `CSP_MOCKUPS` in `functions/_middleware.js`, a relaxed policy with `'unsafe-inline'` on style-src and script-src (mockups are sales previews · self-contained, no shared style system, regenerating hashes per mockup would be churn). The middleware now switches policy based on `URL.pathname.startsWith("/manual-mockups/")`: mockups get `CSP_MOCKUPS`, everything else gets the hash-locked `CSP`. Same guardrails on connect-src (mockups can POST to `mockups.pymewebpro.com/api/*` + Valentina) and frame-ancestors / object-src. **Deploy:** `git add -A && git commit -m "Path-aware CSP for /manual-mockups/" && git push` · Pages auto-deploys in ~30s. **Verify:** open `https://pymewebpro.com/manual-mockups/espacio-dental/` · should now render with full styling. Check DevTools Network → Response Headers for `Content-Security-Policy` containing `'unsafe-inline'`.
- **2026-05-19 (later x12)** · **Mockups moved off the worker · served by pymewebpro.com Pages.** Worker hit Cloudflare's 3MB free-tier size limit (5587KB upload) because `portal/src/manual-mockups-*.js` files bloated to 1-1.7MB each from base64-inlined photos. New architecture: mockup HTML + photos live in `~/code/pymewebpro/manual-mockups/<slug>/` and deploy automatically via the existing pymewebpro.com Cloudflare Pages project on every `git push`. URLs are now `https://pymewebpro.com/manual-mockups/<slug>/`. Worker just 301-redirects `mockups.pymewebpro.com/<slug>/` → the Pages URL so existing share links keep working. Chat / lead / contact API endpoints stay on the worker at `mockups.pymewebpro.com/api/*`; mockup HTML calls them via absolute URLs (`https://mockups.pymewebpro.com/api/...`) with new CORS headers (`Access-Control-Allow-Origin: https://pymewebpro.com`) for the cross-origin POSTs. **Files touched:** `portal/src/manual-mockups.js` (giant import map replaced with `MOCKUP_SLUGS` Set + backwards-compat Proxy for `MANUAL_MOCKUPS[slug]` truthiness checks), `portal/src/mockups.js` (GET handler now redirects, API handlers CORS-wrapped, inviersol.com dead-host returns 404), `portal/package.json` (`check:worker` no longer parses the 10 deleted `manual-mockups-*.js` files), `manual-mockups/<slug>/index.html` × 5 (relative `/api/` → absolute `https://mockups.pymewebpro.com/api/`). New helper: `portal/scripts/_one-off-rewrite-mockup-urls.mjs`. The 10 orphaned `portal/src/manual-mockups-*.js` files are no longer imported (wrangler tree-shakes them out) but the sandbox couldn't delete them · Mike should `rm portal/src/manual-mockups-{blueskitchen,bluewhale,centralfarma,dagaparfum,espaciodental,marena,medellinguide,medellinguideboutique,revocafe,start}.js` when convenient, plus retire `portal/scripts/rebuild-mockups.mjs` (no longer needed). **Bundle size: 5587KB → ~500KB.** **Deploy:** `cd portal && wrangler deploy` (worker), then back at repo root `git add -A && git commit -m "Move mockups to Pages-served URLs" && git push` (Pages deploys revo-cafe + the URL-rewritten HTML). **Verify:** `https://pymewebpro.com/manual-mockups/espacio-dental/` (200 OK), `https://mockups.pymewebpro.com/espacio-dental/` (301 to Pages), `https://colguides.com/portal/pymewebpro/admin#funnel` (SPA mounts).
- **2026-05-19 (later x11)** · **Eliminated the FRONTEND_HTML escape-collision bug class · SPA moved out of the template literal.** Followup to x10. The 3000-line `const FRONTEND_HTML = \`...\`` block in `portal/src/index.js` is the SECOND time this session a stray `\n` (or `\t`, or `\${`) inside the literal silently corrupted the served SPA at worker boot. Real fix: SPA now lives in `portal/src/frontend.html` as a plain file, imported at the top of `index.js` via `import FRONTEND_HTML from "./frontend.html";`. Wrangler bundles it at build time via a new `[[rules]] type="Text"` block in `portal/wrangler.toml`. Zero runtime cost, identical bundle size, but escape-collision is now structurally impossible · template-literal eval no longer touches the SPA bytes. Also added `[build] command = "npm run check"` to `wrangler.toml` so the check fires on EVERY `wrangler deploy` (npm's `predeploy` only fires on `npm run deploy`, which is how x7 slipped past). Updated `scripts/check-spa.mjs` to read `src/frontend.html` directly (with a backstop that falls back to the old template-literal extractor if the file is missing). `scripts/extract-frontend-html.mjs` is the one-off migration tool that produced `frontend.html`. **Deploy:** `cd portal && npm run check && wrangler deploy` · the `[build]` hook will also re-run check during deploy. **Verify:** `https://colguides.com/portal/pymewebpro/admin#funnel` mounts and renders. **Files:** `portal/src/index.js` (replaced template literal block with import), `portal/src/frontend.html` (new, 215KB), `portal/wrangler.toml` ([[rules]] + [build]), `portal/scripts/check-spa.mjs` (reads .html now), `portal/scripts/extract-frontend-html.mjs` (new, one-off).
- **2026-05-19 (later x10)** · **Fixed blank `/portal/pymewebpro/admin` page · unescaped `\n` inside `FRONTEND_HTML` template literal.** `portal/src/index.js` line 3521 had `return lines.join('\n');` inside the inline React JSX (the `buildPrompt()` helper added in "later x7"). Because that line lives inside the `FRONTEND_HTML = \`...\`` template literal, `\n` was eval'd to a real newline at worker boot, then served to the browser as `lines.join('<newline>');` · an unterminated string constant. Babel-standalone threw `Unterminated string constant. (1753:26)` while transpiling, the entire `<script type="text/babel">` block bailed, and `ReactDOM.createRoot(...)` never ran. Symptom: Master Portal header strip renders fine (it's injected outside the React tree), body underneath is blank cream. Fix: `'\n'` → `'\\n'` in the source so the template literal renders `\n` and the browser-side JS reads it as the newline escape. `npm run check:spa` now passes (this is exactly the bug `scripts/check-spa.mjs` was written to catch · the pre-deploy `npm run check` chain runs it, but the x7 deploy must have bypassed `predeploy`). **Deploy:** `cd portal && npm run check && wrangler deploy`. **Verify:** open `https://colguides.com/portal/pymewebpro/admin#funnel`, confirm the SPA mounts and the funnel tab loads. **Follow-up:** double-check `predeploy: npm run check` is actually firing on `wrangler deploy`, since this slipped past once.
- **2026-05-19 (later x9)** · **Traffic-guides series complete · 11 bilingual sub-guides shipped.** Built out the full "Cómo generar tráfico" / "How to drive traffic" series: pillar (#01) plus 10 deep-dives (#02 Google Ads, #03 Meta Ads, #04 SEO local + GBP, #05 WhatsApp Business + catálogo, #06 voz a voz + referidos, #07 email marketing, #08 afiliados, #09 ferias y eventos, #10 prensa local, #11 visibilidad en búsqueda con IA). 22 new files: 11 ES at `guides/<slug>/index.html` and 11 EN at `en/guides/<slug>/index.html` (slugs differ per language: voz-a-voz/word-of-mouth, afiliados/affiliates, ferias-eventos/trade-shows, prensa-local/local-pr, busqueda-ia/ai-search). Each is self-contained HTML matching the pillar's design system (sticky TOC, hero stats grid, channel-meta key/value boxes, do/don't dual columns, numbered step cards, color-coded callouts, comparison tables, dark code blocks, CTA-block dark gradient, series-list cross-linking all 11 entries with current page marked). Visual richness per Mike's request: heavy use of accent color, varied H2/H3/H4 hierarchy, italic Fraunces for emphasis, bold pull-outs, no walls of prose. Topics reshaped from the original NA-leaning roadmap (Reddit, LinkedIn personal brand, newsletter sponsorships dropped) to Colombian-SMB reality (WhatsApp Business, voz a voz, ferias locales, Habeas Data compliance in email guide, Colombian press outlets in PR guide, etc.). All in COP only · ratio-stat exceptions kept per the COP-only rule. Earlier in the day also: added "Cómo conseguir visitantes / How to get visitors" `.nav-pill` to the left of the lang-switch on both homepages + all guide pages, and added acronym-primer `.channel-meta` blocks (CPC, CPM, CTR, CPA, ROAS, AOV, LTV, CAC, plus CBO/CAPI/LAL on Meta) to both pillars + Google Ads + Meta Ads sub-guides. **Build approach:** first 6 files written manually via Write tool, last 11 via a Python templater (`build_guides.py` + `guides_content.py` in scratch outputs dir, not committed) that shares the CSS block and only varies content per guide · saved ~80% of context budget. **Verification:** zero em dashes, zero USD/CAD/dollar references, zero banned words across all 22 files. **Deploy:** `git add -A && git commit -m "Add traffic guides 05-11 (bilingual)" && python3 scripts/refresh-csp-hashes.py && git add functions/_middleware.js && git commit --amend --no-edit && git push` · CSP hook caught 28 new inline `<script>` JSON-LD hashes + 3 inline `<style>` hashes, all auto-added by the refresh script. Live at `pymewebpro.com/guides/<slug>/` and `pymewebpro.com/en/guides/<slug>/`. Pillar series-list at the bottom of both pillars now links all 10 sub-guides (no more "próximamente" / "coming soon"). Commit: `bac6964`.
- **2026-05-19 (later x8)** · **Removed inviersol.com routes from pymewebpro-portal `wrangler.toml`.** Edited `portal/wrangler.toml` to drop `{ pattern = "inviersol.com/*", zone_name = "inviersol.com" }` and `{ pattern = "www.inviersol.com/*", zone_name = "inviersol.com" }`. Replaced with a comment block explaining the trap: inviersol.com now lives on its own dedicated Cloudflare Pages project (`inviersol`, repo `~/code/Inviersol/`), and pymewebpro-portal has zero per-host logic for inviersol, so any deploy with those routes declared causes pymewebpro-portal to hijack inviersol.com and serve empty 200s for every request. Symptom is the entire site going dark while `inviersol.pages.dev` still serves fine. Today's pymewebpro-portal deploy at 19:33 UTC re-attached the routes (wrangler syncs routes from wrangler.toml on every deploy) and took inviersol offline ~10 minutes after we'd brought it back. **Deploy:** no immediate redeploy needed · the manual route deletion in the inviersol.com zone stays in effect as long as no one redeploys pymewebpro-portal from an older checkout of this repo. Next time pymewebpro-portal IS deployed, wrangler will reconcile and the routes will stay gone. See `~/code/Inviersol/CLAUDE.md` Recent changes 2026-05-19 (later) for full incident write-up.
- **2026-05-19 (later x7)** · **Studio admin: "Copy build prompt" button on mockup detail page.** Click → assembles a Cowork-ready prompt from form state (slug, brief, social URLs, owner contact, style brief, iteration notes, chatbot-prompt presence) and writes it to clipboard. Mike clicks → switches to Cowork → pastes → Claude has the full brief inline and follows the playbook without a separate "paste the brief" step. Prompt auto-uses `build` for first-time prospects (no chatbot_system_prompt + status != live) and `revise` for follow-ups, so existing live mockups don't get rebuilt from scratch. New state: `copiedAt` toast for "✓ Build prompt copied to clipboard". File: `portal/src/index.js` MockupProspectDetail · adds `buildPrompt()` + `copyBuildPrompt()` + the button with `<Copy>` icon between Save and Archive, plus an updated hint string. Also patched the build playbook (Step 10) to remind future builds to flip prospect status from `brief` to `live` after first deploy so the listing stays honest. Deploy: `cd portal && wrangler deploy`.
- **2026-05-19 (later x6)** · **CRM WhatsApp templates · replaced 4 generic cold-outreach openers with 5 problem-led versions.** Edited `WA_TEMPLATES` in `portal/src/crm.js` (lines ~3783). Removed: `cold-hot`, `cold-dead`, `cold-hotel`, `cold-restaurant` (all generic "tiene mucho potencial" type pitches). Added: `cold-slow` (sitio carga lento en celular), `cold-no-cta` (no boton de WhatsApp ni CTA clara), `cold-seo` (no aparece bien en Google), `cold-no-site` (no encontre sitio propio), `cold-broken` (sitio no carga bien o desactualizado). Each opens with a specific observable problem on the prospect's site instead of a vague pitch · the hook is "I looked at your site and noticed X" rather than "we can help you." Kept the 4 follow-up templates (after-quote, followup-3day, followup-week, final-touch) untouched. Style matches existing templates: informal "tu", ASCII Spanish (no accents to avoid WA URL-encoding issues), `{business}` substitution, single-line. All include the pymewebpro.com URL at the end. **Deploy:** `cd portal && wrangler deploy`. **Verify:** open colguides.com/portal/pymewebpro/admin/crm, click ▾ next to any contact's WhatsApp button, confirm the picker now shows the 5 new openers + 4 follow-ups.
- **2026-05-19 (later x5)** · **Commit 2 of 3 of the mockup-prospects pipeline · build playbook landed in CLAUDE.md.** Added a "Prospect mockups · build playbook for Claude in Cowork" section (after "Pipeline · manual mockup"). Documents the 11-step procedure a future Claude session follows when it hears `build mockup for <slug>`: get brief from D1 via Mike, study sources, pick bespoke design direction, build HTML, handle images (compress PNGs to JPGs, Imagen text gotchas, CSS overlays), avoid the documented gotchas (aspect-ratio + absolute children collapses, CSP allowlist, stale WA copy), run check-standards, wire into registry, deploy, log to changelog, populate `chatbot_system_prompt` with real FAQ. Captures lessons from the CF build so the next prospect doesn't re-derive them.
- **2026-05-19 (later x4)** · **CF mockup · chat agent is the front door, WhatsApp CTAs replaced.** Nav, hero, and visit-panel buttons no longer dump to wa.me. They open the chat panel via `data-action="open-chat"` (JS extracted `openPanel` to a shared dispatcher). Customer talks to the agent first; the agent decides if/when to escalate to Carlos via the existing `[ORDER]` / `[HANDOFF]` markers → wa.me link. WhatsApp links in the contact info cards + footer stay (those are contact data, not action CTAs). **Substantive system prompt drafted** at `manual-mockups/central-farma-drogueria/chatbot-prompt.md` (~1900 tokens of Spanish brand-aware FAQ: products carried, prices, delivery zones, payment methods, inyectología services + costs, escalation rules). Mock data, plausibly Colombian, marked clearly as approximate so Carlos can replace with real figures. **Studio admin SPA gains a `chatbot_system_prompt` textarea** in the chatbot section of MockupProspectDetail · 14 rows, monospace, with a live char/token counter, so Mike can paste/iterate the prompt from the UI without SQL. Save payload + API already accept the field. **Deploy:** `cd portal && wrangler deploy`. **Next:** Mike pastes the chatbot-prompt.md contents into the new admin textarea on the CF row, saves, tests chat. If the agent still escalates too eagerly, tighten the HANDOFF criteria section of the prompt.
- **2026-05-19 (later x3)** · **CF mockup hero gallery + real logo.** Replaced the SVG monogram-on-green-ring hero graphic with a 4:5 rotating photo carousel (2s crossfade, hover-pause, pauses when tab is hidden, respects `prefers-reduced-motion`). Three Gemini-generated photos: `hero-pharmacist.jpg` (Diana P., handing a box to a customer), `hero-delivery.jpg` (delivery driver lifting helmet next to scooter), `hero-injection.jpg` (Paula R. administering a vaccine). All shot to read as the same studio session. Compressed from ~6MB raw PNG to ~424KB total JPG (q=82, 1000px max) via `convert` in sandbox. **Per-slide overlays** composited only when the delivery slide is active: a white circular CF logo badge (`.crate-badge` at `left:30%, top:62%, width:16%`) sits on the green delivery crate, and a synthetic Colombian motorcycle plate (`.plate-cover` showing "FAR42M", positioned over the AI-garbled original) covers up Imagen's mangled plate text. **Real CF logo wired everywhere.** Logo image `logo-mark.png` (97KB) base64-embedded ONCE via CSS `background-image` on `.brand-mark`, then reused across nav, footer, hero badge, chat avatar, crate overlay. Spans/divs are now empty with `role="img"` + Spanish `aria-label`. **Final bundle: 1125KB** (vs. espacio-dental at 1725KB). Position tuning for crate badge + plate cover is eyeballed · needs visual fine-tune after deploy. **Deploy:** `cd portal && wrangler deploy`. **Note:** original .png hero files are still on disk in `manual-mockups/central-farma-drogueria/` (sandbox can't delete). Mike to `rm hero-*.png` manually.
- **2026-05-19 (later x2)** · **Generic prospect chat agent · Commit 3 of the mockup-prospects pipeline.** Every prospect mockup now gets a real chat agent that answers questions, takes orders, and hands off to the owner by WhatsApp · without per-brand code. Endpoint: `POST mockups.pymewebpro.com/api/chat/<slug>`. **Files added:** `portal/src/prospect-chat.js` (loads `mockup_prospects` row by slug, auto-synthesizes a Spanish system prompt from `business_name` + `style_brief` + `owner_name` + `owner_whatsapp` + `cal_link` unless `chatbot_system_prompt` column is set as a manual override; runs Claude Haiku 4.5; detects `[ORDER: items=...; address=...; phone=...]` and `[HANDOFF: reason=...]` markers in the reply and converts them to a `wa.me` link with a pre-filled message to the owner; returns `{reply, wa_link?, mode}`). **Files changed:** `portal/src/mockups.js` imports + dispatches `handleProspectChat` for any path matching `/api/chat/<slug>` on `mockups.pymewebpro.com`, ordered after the hardcoded espacio-dental matcher so legacy traffic keeps working. **CF mockup wired to the new agent:** replaced the placeholder WhatsApp fab in `manual-mockups/central-farma-drogueria/index.html` with a full chat panel · slide-up modal, brand-green header with "CF" avatar + live indicator, message bubbles, typing dots, error states, WhatsApp CTA when the agent emits an ORDER/HANDOFF marker. Mobile: full-screen modal. Bundle: 32KB → 41KB raw. **Espacio Dental NOT cut over** in this commit · keeps its hardcoded `espacio-dental-chat.js` (different language flow, different booking marker) until a separate migration commit. **Secret:** uses existing `env.ANTHROPIC_API_KEY` already set on the worker. **Deploy:** `cd portal && wrangler deploy`. **Follow-ups:** "Generate system prompt" button in the studio admin SPA (let Mike review/edit the auto-synthesized prompt before going live); cut espacio-dental over to the generic handler once we've verified the Spanish flow on CF; rate limiting per IP; "Open in WhatsApp now" passthrough button at top of chat for users who just want to skip the bot.
- **2026-05-19 (later x1)** · **First prospect mockup built via the new workflow · `central-farma-drogueria`.** Brief came in through the studio admin (status: brief, IG @centralfarmamed, owner Carlos, WA 573023877739). Built bespoke at `manual-mockups/central-farma-drogueria/index.html` (~31KB raw, 32KB bundled). Spanish-only (neighborhood market, not expats). Type-and-icon driven (no photos) to match their actual IG aesthetic which is graphic-driven, hit Lighthouse-100 prereqs, and keep page weight tiny. Sections: nav · hero with CF monogram in brand-green ring · 3-card services (domicilios, inyectología, productos) · inyectología deep-dive with "qué traer" checklist · 3-step domicilios + coverage pills · address/hours/WA/IG cards + visit CTA · footer · floating WA fab. JSON-LD: Pharmacy + Service (Inyectología) + OpeningHoursSpecification. Brand palette extracted from CF wordmark (`#2E8B6A` green, `#0E2A4D` navy, `#D7E8F4` soft blue, `#F8EFD4` cream). Type pairing: Fraunces (display, opsz 144) + Inter (body), distinct from existing 8 mockups. WhatsApp fab is a placeholder · gets replaced with the generic chat agent in Commit 3 of the mockup-prospects pipeline. **Files added:** `manual-mockups/central-farma-drogueria/index.html`, `portal/src/manual-mockups-centralfarma.js` (auto-generated by rebuild-mockups). **Files changed:** `portal/src/manual-mockups.js` (imported + registered slug), `portal/src/chief-of-staff.js` (added to `toolListMockups` static list). Quality gate: `check-standards` passes 0 FAILs · only the standard "Has Organization JSON-LD" WARN that affects 5 other production mockups (Pharmacy is a schema.org subtype of Organization, so this is regex-only and not a real SEO issue). **Deploy:** `cd portal && wrangler deploy`. **Follow-ups:** in the admin form, flip status to `live`. Mike + Carlos to confirm payment-method copy on the domicilios section (currently says "tú decides cómo pagar al recibir", neutral) and the inyectología pricing line (currently honest "consulta el costo por WhatsApp"). Phase 2 hardening: per-mockup HTTP-header CSP via mockups.js (currently only `<meta>` CSP via inline tags possible).
- **2026-05-19** · **Mockup-prospects pipeline · Commit 1 of 3.** New workflow for sales-prospect mockups: Mike fills in a brief in the studio admin (`/portal/pymewebpro/admin` → Mockups tab), then asks Claude in Cowork to build the bespoke site at `manual-mockups/<slug>/index.html`. The portal is a brief tracker, NOT a generator · per Mike's quality bar, every site is hand-built in Cowork to bespoke + A+ security + 100 Lighthouse + <1s load. **Files added:** `portal/migrations/0008_mockup_prospects.sql` (new `mockup_prospects` table: slug, business_name, instagram_url, facebook_url, tiktok_url, website_url, style_brief, owner_name, owner_whatsapp, cal_link, notes, chatbot_system_prompt, status, created_at, updated_at; seeds an `espacio-dental` row for the Commit 3 chat cutover), `portal/src/mockup-prospects.js` (CRUD handler exporting `handleMockupProspects`; admin-only; soft-delete via `status='archived'`). **Files changed:** `portal/src/mockups.js` imports + dispatches `handleMockupProspects` before the existing `/api/admin/mockups` matchers. `portal/src/index.js` SPA gains a Mockups tab (Palette icon) with list view, new-mockup modal, and full detail/edit page (brief fields + chatbot fields + iteration-notes textarea). New state: `mockups`, `mockupDetail`, `showNewMockup`. New routes: `/admin/mockups`, `/admin/mockups/<slug>`. New components: `MockupRow`, `NewMockupModal`, `MockupProspectDetail`. **Apply + deploy:** `cd portal && wrangler d1 execute pymewebpro-portal --remote --file=migrations/0008_mockup_prospects.sql && wrangler deploy`. **Commit 2 (next):** Cowork-side build playbook lives in CLAUDE.md so Claude can build mockups from a brief by slug. **Commit 3:** generic `prospect-chat.js` reads brand profile from `mockup_prospects` by slug, replacing the hardcoded `espacio-dental-chat.js`.
- **2026-05-18** · Section 4 testimonials + builds-grid + flattened build:bilingual indirection. Added Santiago Florez (Inviersol) as third Google review on pymewebpro.com (ES + EN), switched section 4 grid to 3 columns desktop/tablet, smaller card padding/font, single column on mobile. Pointed Schedulator build tile at `schedulator2.vercel.app` and added Inviersol tile pointing at `inviersol.com` (both live clients, not mockups). **Removed slugs from `MANUAL_MOCKUPS`:** `schedulator`, `inviersol`, `pymewebpro-ca`, `pymewebpro-v1`, `pymewebpro-v3`, `pymewebpro-v3-es`, `pymewebpro-v3-fr`, `pymewebpro-v4`, `pymewebpro-v4-es`. Registry now 9 slugs (6 client/concept mockups + 3 medellin-guide variants + start). **Flattened production build:** root `index.html` + `en/index.html` are now the actual source of truth. Deleted the indirection: removed `npm run build:bilingual` from the `check` chain. `check:standards` now validates the root files in-place as "pymewebpro.com (ES)" and "(EN)" entries. CLAUDE.md was previously stale: it claimed `manual-mockups/pymewebpro-ca/index.html` was source-of-truth when build:bilingual actually sourced from `pymewebpro-v4`/`-es`. Cost a wasted hour. **DNS:** Mike re-added a proxied A record for `mockups.pymewebpro.com` (was missing entirely, which had killed every "See the build" tile on the homepage). **Files Mike still needs to `rm` manually** (cowork sandbox can't delete): `manual-mockups/{inviersol,schedulator,pymewebpro-ca,pymewebpro-v1,pymewebpro-v3,pymewebpro-v3-es,pymewebpro-v3-fr,pymewebpro-v4,pymewebpro-v4-es}/`, `scripts/{build-bilingual,promote-v4}.mjs`, `portal/src/manual-mockups-{inviersol,schedulator,pymewebproca,pymewebprov1,pymewebprov3,pymewebprov3es,pymewebprov3fr,pymewebprov4,pymewebprov4es}.js`. Also synced the static slug list in `portal/src/chief-of-staff.js` `toolListMockups()` with the new registry. Deploy: `cd portal && wrangler deploy` (worker), `git push` (Pages).
