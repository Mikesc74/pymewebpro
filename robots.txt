# PymeWebPro — robots.txt
# Marketing site is fully indexable. The portal lives on a different host
# (portal.pymewebpro.com) so we don't gate it from here.

User-agent: *
Allow: /
Disallow: /api/
Disallow: /functions/

# Common AI crawlers — allow factual indexing, no special handling
# (add Disallow rules per crawler here if policy changes)

Sitemap: https://pymewebpro.com/sitemap.xml
